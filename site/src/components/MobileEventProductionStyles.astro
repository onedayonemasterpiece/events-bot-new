<style is:global>
  [data-mobile-event-production] .event-hero__meta-line--weekday-panel { display: none; }

  @media (max-width: 1023px) {
    [data-mobile-event-production] .mobile-event-production__shell { padding-bottom: 1.5rem; }
    [data-mobile-event-production] .section-card--impact-hero {
      width: 100vw;
      margin-left: calc(50% - 50vw);
      margin-right: calc(50% - 50vw);
    }
    [data-mobile-event-production] .event-hero__meta-line--standard { display: none; }
    [data-mobile-event-production] .event-hero__meta-line--weekday-panel {
      display: grid;
      gap: 0.58rem;
      margin-top: 0.08rem;
      padding: 0.78rem 0.08rem 0.82rem;
      border-top: 1px solid rgba(121, 48, 20, 0.22);
      border-bottom: 1px solid rgba(121, 48, 20, 0.14);
      border-radius: 0;
      background: transparent;
      color: #34271f;
      box-shadow: none;
    }
    [data-mobile-event-production] .event-hero__meta-line--weekday-panel .event-hero__when {
      display: grid;
      grid-template-columns: auto minmax(0, 1fr) auto;
      align-items: center;
      gap: 0.5rem;
      line-height: 1;
    }
    [data-mobile-event-production] .event-hero__weekday {
      display: inline-flex;
      min-height: 30px;
      align-items: center;
      justify-content: center;
      padding: 0.35rem 0.54rem;
      border-radius: 9px;
      background: #b54d22;
      color: #fffaf2;
      font-size: 0.74rem;
      font-weight: 950;
      letter-spacing: 0.07em;
      text-transform: uppercase;
    }
    [data-mobile-event-production] .event-hero__date {
      color: #7e2d12;
      font-size: 1.12rem;
      font-weight: 950;
      letter-spacing: -0.025em;
    }
    [data-mobile-event-production] .event-hero__time {
      color: #241c17;
      font-size: 1.2rem;
      font-weight: 950;
      font-variant-numeric: tabular-nums;
      letter-spacing: -0.025em;
    }
    [data-mobile-event-production] .event-hero__meta-line--weekday-panel .event-hero__where {
      padding-top: 0.55rem;
      border-top: 1px solid rgba(121, 48, 20, 0.14);
      font-size: 0.92rem;
      font-weight: 820;
      line-height: 1.22;
    }
    [data-mobile-event-production] .event-hero--poster-stage .event-hero__visual {
      transform: translateX(-50%) !important;
      overflow: hidden;
      will-change: auto;
    }
    [data-mobile-event-production] .event-hero--poster-stage .event-hero__image {
      margin-bottom: calc(-1 * var(--hero-poster-travel, 42px));
      transform: translate3d(0, var(--hero-poster-parallax-y, 0px), 0);
      will-change: transform;
    }
    [data-mobile-event-production][data-mobile-parallax-profile="photo-continuous-crop"] .event-hero--poster-stage .event-hero__image {
      margin-top: calc(-1 * var(--hero-poster-travel, 42px));
    }
    [data-mobile-event-production][data-mobile-review-variant="accepted-v8"] .event-hero__decision {
      border: 0;
      box-shadow: none;
    }
    [data-mobile-event-production][data-mobile-review-variant="accepted-v8"] .mobile-event-production__continuation {
      --continuation-rise: clamp(28rem, 110vw, 32rem);
      position: relative;
      z-index: 1;
      isolation: isolate;
      display: block;
      width: 100vw;
      margin-top: -1.5rem;
      margin-left: calc(50% - 50vw);
      margin-right: calc(50% - 50vw);
      padding: calc(1.5rem + clamp(0.6rem, 2.8vw, 0.75rem)) 0.75rem 0;
      box-sizing: border-box;
      border: 0;
      border-radius: 0;
      background: none;
      box-shadow: none;
    }
    [data-mobile-event-production][data-mobile-review-variant="accepted-v8"] .mobile-event-production__continuation::before {
      content: "";
      position: absolute;
      z-index: -1;
      left: 0;
      right: 0;
      top: calc(-1 * var(--continuation-rise));
      height: calc(var(--continuation-rise) + 44rem);
      pointer-events: none;
      background: linear-gradient(
        180deg,
        rgba(242,231,215,0) 0,
        rgba(242,231,215,0.22) calc(var(--continuation-rise) - 21rem),
        rgba(240,225,207,0.7) calc(var(--continuation-rise) - 12rem),
        rgba(250,240,226,0.9) var(--continuation-rise),
        rgba(255,249,240,0.82) calc(var(--continuation-rise) + 16rem),
        rgba(248,240,229,0) calc(var(--continuation-rise) + 44rem)
      );
    }
    [data-mobile-event-production] .mobile-event-production__medallions {
      margin-top: 0;
    }
    [data-mobile-event-production] .mobile-event-production__details {
      margin-top: clamp(1.1rem, 4.6vw, 1.35rem) !important;
      overflow: visible;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
    }
    [data-mobile-event-production] .mobile-event-production__prose {
      width: min(100%, 66ch);
      margin-inline: auto;
      padding-inline: 0.25rem;
      padding-top: 1.15rem;
      padding-bottom: 0.35rem;
    }
    [data-mobile-event-production] .event-source-gate--section {
      width: min(100%, 66ch);
      margin: 0.85rem auto 0;
      border: 1px solid rgba(121, 48, 20, 0.12);
      border-radius: 16px;
      background: rgba(255, 253, 248, 0.52);
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      align-items: stretch;
      gap: 0.48rem;
      padding: 0.58rem;
      border: 1px solid rgba(255, 248, 239, 0.1);
      border-radius: 20px;
      background:
        radial-gradient(110% 85% at 50% -18%, rgba(255, 255, 255, 0.12) 0%, rgba(255, 255, 255, 0) 68%),
        linear-gradient(175deg, #302823 0%, #1e1915 100%);
      box-shadow:
        0 18px 36px -8px rgba(35, 27, 22, 0.42),
        inset 0 1px 0 rgba(255, 255, 255, 0.12),
        inset 0 -1px 0 rgba(0, 0, 0, 0.46);
      container-type: inline-size;
      container-name: mobile-action-dock;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .cta-button {
      grid-column: 1 / -1;
      width: 100%;
      min-height: 54px;
      border-color: rgba(255, 199, 164, 0.22);
      border-radius: 13px;
      background: linear-gradient(180deg, #be552a 0%, #9a3f1d 100%);
      color: #fffaf2;
      box-shadow:
        inset 0 1px 1px rgba(255, 255, 255, 0.34),
        inset 0 -2px 4px rgba(63, 20, 7, 0.28),
        0 5px 14px rgba(133, 47, 17, 0.38);
      text-shadow: 0 1px 1px rgba(62, 22, 8, 0.34);
      transition: background 180ms ease, border-color 180ms ease, box-shadow 180ms ease, transform 160ms ease;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .secondary-button {
      width: 100%;
      min-width: 0;
      min-height: 52px;
      padding: 0.55rem 0.4rem;
      border-color: rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      background: linear-gradient(180deg, #3b332d 0%, #29231f 100%);
      color: #eee3d4;
      box-shadow:
        inset 0 1px 1px rgba(255, 255, 255, 0.12),
        inset 0 -1px 1px rgba(0, 0, 0, 0.28),
        0 3px 6px rgba(0, 0, 0, 0.28);
      font-size: 0.84rem;
      line-height: 1.05;
      text-shadow: 0 1px 1px rgba(0, 0, 0, 0.32);
      transition: background 180ms ease, border-color 180ms ease, box-shadow 180ms ease, color 180ms ease, transform 160ms ease;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .cta-button--disabled {
      grid-column: 1 / -1;
    }
    @media (hover: hover) and (pointer: fine) {
      [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .cta-button:hover {
        border-color: rgba(255, 210, 184, 0.34);
        background: linear-gradient(180deg, #ca6131 0%, #a84720 100%);
        box-shadow:
          inset 0 1px 1px rgba(255, 255, 255, 0.4),
          inset 0 -2px 4px rgba(63, 20, 7, 0.24),
          0 7px 18px rgba(169, 64, 26, 0.46);
        transform: translateY(-1px);
      }
      [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .secondary-button:hover {
        border-color: rgba(255, 255, 255, 0.18);
        background: linear-gradient(180deg, #473d35 0%, #322a24 100%);
        color: #fffaf2;
        box-shadow:
          inset 0 1px 1px rgba(255, 255, 255, 0.16),
          0 5px 10px rgba(0, 0, 0, 0.3);
        transform: translateY(-1px);
      }
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .cta-button:active {
      border-color: rgba(255, 183, 143, 0.2);
      background: #8d3718;
      box-shadow: inset 0 4px 7px rgba(47, 14, 4, 0.52);
      transform: translateY(1px);
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .secondary-button:active {
      border-color: rgba(255, 255, 255, 0.06);
      background: #211b17;
      box-shadow: inset 0 3px 5px rgba(0, 0, 0, 0.5);
      transform: translateY(1px);
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > :is(a, button):focus-visible {
      outline: 3px solid #5eead4;
      outline-offset: 2px;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like {
      position: relative;
      min-height: 52px;
      overflow: hidden;
      padding-inline: 0.4rem;
      color: #eee3d4;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like[aria-pressed="true"] {
      border-color: rgba(255, 110, 132, 0.58) !important;
      background: linear-gradient(180deg, rgba(170, 42, 63, 0.92), rgba(108, 25, 43, 0.96)) !important;
      color: #ffd8df !important;
      box-shadow:
        inset 0 1px 1px rgba(255, 255, 255, 0.18),
        inset 0 -1px 2px rgba(44, 4, 14, 0.45),
        0 0 0 1px rgba(255, 102, 128, 0.18),
        0 4px 12px rgba(153, 30, 52, 0.34) !important;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like[aria-pressed="true"] .icon__heart-outline { display: none; }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like[aria-pressed="true"] .icon__heart-solid { display: block !important; }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > [data-calendar-action].is-calendar-added,
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > [data-calendar-action].is-calendar-added:hover {
      border-color: rgba(94, 234, 212, 0.54) !important;
      background: linear-gradient(180deg, rgba(18, 113, 105, 0.94), rgba(10, 74, 70, 0.98)) !important;
      color: #cffff7 !important;
      box-shadow:
        inset 0 1px 1px rgba(255, 255, 255, 0.17),
        inset 0 -1px 2px rgba(1, 38, 35, 0.46),
        0 0 0 1px rgba(45, 212, 191, 0.16),
        0 4px 12px rgba(13, 118, 110, 0.32) !important;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions .icon { width: 1.28rem; height: 1.28rem; }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions .feedback-count:empty { display: none; }
  }

  @container mobile-action-dock (max-width: 400px) {
    [data-mobile-event-production] .event-hero__actions > .secondary-button {
      padding-inline: 0.32rem;
    }
    [data-mobile-event-production] .event-hero__actions > .secondary-button > span:not(.feedback-count) { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > :is(.cta-button, .secondary-button) {
      transition: none;
      transform: none;
    }
  }
</style>
