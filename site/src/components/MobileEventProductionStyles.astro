<style is:global>
  [data-mobile-event-production] .event-hero__meta-line--weekday-panel { display: none; }

  @media (max-width: 1023px) {
    [data-mobile-event-production] .mobile-event-production__shell { padding-bottom: 1.5rem; }
    [data-mobile-event-production] .section-card--impact-hero {
      width: 100vw;
      margin-left: calc(50% - 50vw);
      margin-right: calc(50% - 50vw);
    }
    [data-mobile-event-production] .event-hero__meta-line--standard { display: none; }
    [data-mobile-event-production] .event-hero__meta-line--weekday-panel {
      display: grid;
      gap: 0.58rem;
      margin-top: 0.08rem;
      padding: 0.78rem 0.08rem 0.82rem;
      border-top: 1px solid rgba(121, 48, 20, 0.22);
      border-bottom: 1px solid rgba(121, 48, 20, 0.14);
      border-radius: 0;
      background: transparent;
      color: #34271f;
      box-shadow: none;
    }
    [data-mobile-event-production] .event-hero__meta-line--weekday-panel .event-hero__when {
      display: grid;
      grid-template-columns: auto minmax(0, 1fr) auto;
      align-items: center;
      gap: 0.5rem;
      line-height: 1;
    }
    [data-mobile-event-production] .event-hero__weekday {
      display: inline-flex;
      min-height: 30px;
      align-items: center;
      justify-content: center;
      padding: 0.35rem 0.54rem;
      border-radius: 9px;
      background: #b54d22;
      color: #fffaf2;
      font-size: 0.74rem;
      font-weight: 950;
      letter-spacing: 0.07em;
      text-transform: uppercase;
    }
    [data-mobile-event-production] .event-hero__date {
      color: #7e2d12;
      font-size: 1.12rem;
      font-weight: 950;
      letter-spacing: -0.025em;
    }
    [data-mobile-event-production] .event-hero__time {
      color: #241c17;
      font-size: 1.2rem;
      font-weight: 950;
      font-variant-numeric: tabular-nums;
      letter-spacing: -0.025em;
    }
    [data-mobile-event-production] .event-hero__meta-line--weekday-panel .event-hero__where {
      padding-top: 0.55rem;
      border-top: 1px solid rgba(121, 48, 20, 0.14);
      font-size: 0.92rem;
      font-weight: 820;
      line-height: 1.22;
    }
    [data-mobile-event-production] .event-hero--poster-stage .event-hero__visual {
      transform: translateX(-50%) !important;
      overflow: visible;
      will-change: auto;
    }
    [data-mobile-event-production] .event-hero--poster-stage .event-hero__image {
      margin-top: 0;
      margin-bottom: 0;
      transform: none;
      will-change: auto;
    }
    [data-mobile-event-production][data-mobile-parallax-profile="photo-continuous-crop"] .event-hero--poster-stage .event-hero__image {
      margin-top: 0;
    }
    [data-mobile-event-production][data-mobile-review-variant="accepted-v8"] .event-hero__decision {
      border: 0;
      box-shadow: none;
    }
    [data-mobile-event-production][data-mobile-review-variant="accepted-v8"] .mobile-event-production__continuation {
      --continuation-rise: clamp(28rem, 110vw, 32rem);
      position: relative;
      z-index: 1;
      isolation: isolate;
      display: block;
      width: 100vw;
      margin-top: -1.5rem;
      margin-left: calc(50% - 50vw);
      margin-right: calc(50% - 50vw);
      padding: calc(1.5rem + clamp(0.6rem, 2.8vw, 0.75rem)) 0.75rem 0;
      box-sizing: border-box;
      border: 0;
      border-radius: 0;
      background: none;
      box-shadow: none;
    }
    [data-mobile-event-production][data-mobile-review-variant="accepted-v8"] .mobile-event-production__continuation::before {
      content: "";
      position: absolute;
      z-index: -1;
      left: 0;
      right: 0;
      top: calc(-1 * var(--continuation-rise));
      height: calc(var(--continuation-rise) + 44rem);
      pointer-events: none;
      background: linear-gradient(
        180deg,
        rgba(242,231,215,0) 0,
        rgba(242,231,215,0.22) calc(var(--continuation-rise) - 21rem),
        rgba(240,225,207,0.7) calc(var(--continuation-rise) - 12rem),
        rgba(250,240,226,0.9) var(--continuation-rise),
        rgba(255,249,240,0.82) calc(var(--continuation-rise) + 16rem),
        rgba(248,240,229,0) calc(var(--continuation-rise) + 44rem)
      );
    }
    [data-mobile-event-production] .mobile-event-production__medallions {
      margin-top: 0;
      width: 100%;
      min-width: 0;
      max-width: 100%;
    }
    [data-mobile-event-production] .mobile-event-production__medallions .event-token-row {
      width: 100%;
      min-width: 0;
      max-width: 100%;
      gap: clamp(0.55rem, 2.4vw, 0.7rem);
    }
    [data-mobile-event-production] .mobile-event-production__medallions .event-token:not(.event-token--pill) {
      /* One mobile identity scale: Main/Secondary changes placement priority,
         never the apparent physical size of organizer/program/fact artwork. */
      --token-size: clamp(84px, 23vw, 92px);
    }
    [data-mobile-event-production] .mobile-event-production__medallions .event-token,
    [data-mobile-event-production] .mobile-event-production__medallions .event-token__circle,
    [data-mobile-event-production] .mobile-event-production__medallions .event-token__pushkin-frame {
      max-width: 100%;
    }
    [data-mobile-event-production] .mobile-event-production__medallions .event-token__circle img,
    [data-mobile-event-production] .mobile-event-production__medallions .event-token__pushkin-composite {
      max-width: 100%;
    }
    [data-mobile-event-production] .mobile-event-production__details {
      margin-top: clamp(1.1rem, 4.6vw, 1.35rem) !important;
      overflow: visible;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
    }
    [data-mobile-event-production] .mobile-event-production__prose {
      width: min(100%, 66ch);
      margin-inline: auto;
      padding-inline: 0.25rem;
      padding-top: 1.15rem;
      padding-bottom: 0.35rem;
    }
    [data-mobile-event-production] .event-source-gate--section {
      width: min(100%, 66ch);
      margin: 0.85rem auto 0;
      border: 1px solid rgba(121, 48, 20, 0.12);
      border-radius: 16px;
      background: rgba(255, 253, 248, 0.52);
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions {
      display: flex;
      flex-wrap: wrap;
      align-items: stretch;
      gap: 0.42rem;
      padding: 0.48rem;
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 18px;
      background: #292521;
      box-shadow: 0 14px 28px rgba(41, 37, 33, 0.18);
      container-type: inline-size;
      container-name: mobile-action-dock;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .cta-button {
      flex: 1 0 100%;
      width: 100%;
      min-height: 52px;
      border-radius: 14px;
      box-shadow: none;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .secondary-button {
      flex: 1 1 7.15rem;
      width: auto;
      min-width: 0;
      min-height: 48px;
      padding: 0.55rem 0.48rem;
      border-color: rgba(255, 255, 255, 0.1);
      border-radius: 13px;
      background: rgba(255, 255, 255, 0.08);
      color: #fffaf2;
      box-shadow: none;
      font-size: 0.84rem;
      line-height: 1.05;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .secondary-button:hover {
      border-color: rgba(255, 255, 255, 0.2);
      background: rgba(255, 255, 255, 0.13);
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like {
      position: relative;
      flex: 0 0 56px;
      width: 56px;
      min-height: 52px;
      overflow: hidden;
      padding-inline: 0.42rem;
      color: #fffaf2;
      transition: color 180ms ease, background 180ms ease, border-color 180ms ease;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like[aria-pressed="true"] {
      border-color: transparent !important;
      background: #b54d22 !important;
      color: #fff !important;
      box-shadow: none !important;
      transform: none;
    }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like[aria-pressed="true"] .icon__heart-outline { display: none; }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like[aria-pressed="true"] .icon__heart-solid { display: block !important; }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions .icon { width: 1.28rem; height: 1.28rem; }
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions .feedback-count:empty { display: none; }
  }

  @media (max-width: 379px) {
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .secondary-button:not(.feedback-button--like) {
      flex-basis: 5.9rem;
      padding-inline: 0.34rem;
      font-size: 0.79rem;
    }
  }

  @container mobile-action-dock (max-width: 400px) {
    [data-mobile-event-production] .event-hero__actions > .secondary-button:not([data-native-share]) {
      flex: 0 0 52px;
      width: 52px;
      padding-inline: 0.38rem;
    }
    [data-mobile-event-production] .event-hero__actions > .secondary-button:not([data-native-share]) > span:not(.feedback-count) { display: none; }
    [data-mobile-event-production] .event-hero__actions > [data-native-share] {
      flex:1 1 9rem;
      width:auto;
      min-width:8.5rem;
    }
    [data-mobile-event-production] .event-hero__actions > [data-native-share] [data-share-label] { display:inline; }
  }

  @media (prefers-reduced-motion: reduce) {
    [data-mobile-event-production][data-actions-treatment="dock"] .event-hero__actions > .feedback-button--like { transition: none; }
  }
</style>
