# Badge source provenance

## Pushkin card

`pushkin-card.item_2637.jpg` is the source image for the local Pushkin-card medallion pipeline. See `docs/features/static-site-pages/event-token-medallions.md` for the rendering contract.

`Бахослужение` now uses the source-grounded festival medallion asset under `site/src/assets/festivals/source/` and runtime `/assets/festivals/bahosluzhenie.png`; no separate deterministic badge asset is used.
