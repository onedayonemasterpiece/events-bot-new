---
import EventLayout from '../../../layouts/EventLayout.astro';
import Icon from '../../../components/Icon.astro';
import MedallionStrip from '../../../components/lab/DecisionMedallionStrip.astro';
import { absoluteUrl, SITE_NAME, withBase } from '../../../lib/events';

const canonicalUrl = absoluteUrl('/lab/event-decision-block/');
const title = `Лаборатория страницы события V3 | ${SITE_NAME}`;
const description = 'Mobile-first варианты decision-sheet страницы события: компактная сильная дата, точный адрес, цена или бесплатно, icon+count действия и крупные прокручиваемые медальоны.';
const baselineCommit = '63e88020';
const currentBranch = 'feature/event-page-ux-lab-v3-20260710';
const previewLinks = [
  { label: 'Сохранённый блок даты', href: withBase('/lab/date-block/') },
  { label: 'Полный список медальонов', href: withBase('/lab/medallions/') },
  { label: 'Hero review', href: withBase('/lab/hero/review/') },
];

const sample = {
  title: 'Органный вечер при свечах',
  type: 'концерт',
  day: '05',
  month: 'июля',
  weekday: 'суббота',
  weekdayShort: 'сб',
  time: '19:00',
  venue: 'Кафедральный собор',
  address: 'ул. Канта, 1 · Калининград',
  price: '600–1 500 ₽',
  primaryCta: 'Купить билет',
  datetime: '2026-07-05T19:00:00+02:00',
};

const metrics = [
  { icon: 'calendar' as const, value: '+24', label: 'Добавить в календарь, добавлено 24 раза' },
  { icon: 'share' as const, value: '118', label: 'Поделиться событием, поделились 118 раз' },
  { icon: 'heart' as const, value: '347', label: 'Поставить отметку «Нравится», нравится 347 людям' },
];

const medallions = [
  { label: 'Бахослужение', short: 'BACH', image: '/assets/festivals/bahosluzhenie.png', tone: 'gold' },
  { label: 'Пушкинская карта', short: 'ПК', image: '/assets/badges/pushkin-card-medallion.webp', tone: 'green' },
  { label: 'Остров Канта', short: 'OK', image: '/assets/organizers/kant-island.webp', tone: 'blue' },
  { label: 'Источник проверен', short: 'SRC', image: '', tone: 'neutral' },
];

const principles = [
  ['Один главный CTA', 'Покупка или регистрация — единственная залитая кнопка. Остальные действия компактны, но не спрятаны.'],
  ['Сильная, не обязательно широкая дата', 'Проверяем компактный tile, calm bar и split-band против сохранённой full-width даты.'],
  ['Только полезный admission', 'Цена или диапазон заменяют «по билетам». Бесплатно всегда явно. При неизвестной цене слот исчезает.'],
  ['Точный адрес', 'Площадка и улица читаются вместе; допускаются 1–3 строки без жёсткого однострочного обрезания.'],
  ['Крупные медальоны', '90–108px, отдельная горизонтальная shelf с peek; при нуле блок полностью схлопывается.'],
];

const supersededMarkers = [
  'decision-variant-v2-medallion-shelf',
  'decision-variant-v2-attached-medallions',
  'decision-variant-v2-zero-medallions',
  'decision-variant-p03',
  'decision-variant-p01',
  'decision-variant-p04',
  'decision-variant-baseline',
  'decision-variant-p03-zero-medallions',
  'decision-variant-utility-stack',
];
---

<EventLayout title={title} description={description} canonicalUrl={canonicalUrl} structuredData={[]}>
  <main id="main" class="decision-lab decision-lab-v3" data-event-decision-lab data-decision-v3-overlap-lab>
    <div class="page-shell decision-lab__shell">
      <nav class="crumbs" aria-label="Хлебные крошки">
        <a href={withBase('/__preview/')}>Афиша</a><span aria-hidden="true">/</span><span>Decision-блок V3</span>
      </nav>

      <section class="section-card decision-lab__intro">
        <div class="section-card__inner decision-lab__intro-inner">
          <p class="eyebrow">A/B/C/D review · mobile first · baseline preserved</p>
          <h1>Сильная дата без перегруженного первого экрана</h1>
          <p class="lead">Hero-overlap, H1 внутри sheet и крупные медальоны не меняются. В этой итерации убраны три длинные подписи вторичных действий, «по билетам» заменено реальной ценностью, добавлен точный адрес и проверены более компактные способы держать дату сильной.</p>
          <div class="decision-lab__meta-grid" aria-label="Контекст исследования">
            <div><span>сохранённый контроль</span><code>{baselineCommit}</code></div>
            <div><span>ветка лаборатории</span><strong>{currentBranch}</strong></div>
            <div><span>исследование</span><strong>120 референсов · Gemini 3.1 Pro · a-opus</strong></div>
          </div>
          <div class="decision-lab__links" aria-label="Связанные лаборатории">
            {previewLinks.map((item) => <a href={item.href}>{item.label}</a>)}
          </div>
        </div>
      </section>

      <section class="section-card">
        <div class="section-card__inner">
          <div class="decision-lab__section-head">
            <p class="eyebrow">Общий контракт всех новых вариантов</p>
            <h2>Сравниваем компоновку, а не ломаем уже принятые элементы</h2>
          </div>
          <div class="decision-lab__criteria-grid">
            {principles.map(([heading, text]) => <article><h3>{heading}</h3><p>{text}</p></article>)}
          </div>
        </div>
      </section>

      <section class="decision-lab__variants decision-lab__variants--v3" aria-label="Новые варианты decision-блока">
        <article class="section-card decision-lab__variant decision-variant-v3-compact-tile" data-variant="compact-tile">
          <div class="section-card__inner">
            <div class="decision-lab__variant-head">
              <p class="eyebrow">V3-A · compact date · Codex/Pinterest synthesis</p>
              <h2>Compact Date Tile</h2>
              <p>Самый компактный сильный якорь: квадрат даты занимает левую часть строки, справа остаются H1 и цена. На 320px tile становится чуть уже, но не прыгает на отдельную строку.</p>
            </div>
            <div class="decision-phone">
              <div class="decision-hero decision-hero--amber"><span>parallax hero image</span></div>
              <section class="decision-sheet">
                <p class="decision-eyebrow"><span>{sample.type}</span><strong class="admission admission--price">от 600 ₽</strong></p>
                <div class="compact-date-title">
                  <time class="compact-date-tile" datetime={sample.datetime} aria-label={`${sample.day} ${sample.month}, ${sample.weekday}, ${sample.time}`}>
                    <b>{sample.day}</b><span>{sample.month}</span><small>{sample.weekdayShort} · {sample.time}</small>
                  </time>
                  <h3>{sample.title}</h3>
                </div>
                <p class="decision-place"><Icon name="pin" /><span><strong>{sample.venue}</strong><small>{sample.address}</small></span></p>
                <a class="decision-primary" href="#review"><Icon name="ticket" /><span>{sample.primaryCta}</span></a>
                <div class="decision-metrics action-metric-row" aria-label="Действия и показатели события">
                  {metrics.map((metric) => <button type="button" aria-label={metric.label} title={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                </div>
              </section>
              <div class="decision-medallions large-medallion-strip"><MedallionStrip items={medallions} mode="large" /></div>
            </div>
            <div class="decision-lab__variant-notes"><p><strong>Плюс:</strong> дата остаётся визуальным знаком, но экономит около 45–55px высоты.</p><p><strong>Риск:</strong> длинный H1 справа от tile может стать на строку выше, чем в остальных вариантах.</p></div>
          </div>
        </article>

        <article class="section-card decision-lab__variant decision-variant-v3-calm-bar" data-variant="calm-bar">
          <div class="section-card__inner">
            <div class="decision-lab__variant-head">
              <p class="eyebrow">V3-B · consultant consensus · production-first</p>
              <h2>Calm Date Bar</h2>
              <p>Самый безопасный вариант по итогам a-opus и Gemini: title-first, затем однострочная дата с крупным числом, полный адрес, CTA и спокойная строка icon+count.</p>
            </div>
            <div class="decision-phone">
              <div class="decision-hero decision-hero--forest"><span>parallax hero image</span></div>
              <section class="decision-sheet">
                <p class="decision-eyebrow"><span>{sample.type}</span><strong class="admission admission--price">{sample.price}</strong></p>
                <h3>{sample.title}</h3>
                <time class="calm-date-bar" datetime={sample.datetime} aria-label={`${sample.day} ${sample.month}, ${sample.weekday}, ${sample.time}`}>
                  <b>{sample.day}</b><span>{sample.month}</span><em>· {sample.weekdayShort}</em><strong>· {sample.time}</strong>
                </time>
                <p class="decision-place"><Icon name="pin" /><span><strong>{sample.venue}</strong><small>{sample.address}</small></span></p>
                <a class="decision-primary" href="#review"><Icon name="ticket" /><span>{sample.primaryCta}</span></a>
                <div class="decision-metrics action-metric-row" aria-label="Действия и показатели события">
                  {metrics.map((metric) => <button type="button" aria-label={metric.label} title={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                </div>
              </section>
              <div class="decision-medallions large-medallion-strip"><MedallionStrip items={medallions} mode="large" /></div>
            </div>
            <div class="decision-lab__variant-notes"><p><strong>Плюс:</strong> лучший баланс предсказуемости, доступности и экономии места.</p><p><strong>Риск:</strong> менее характерный, чем tile; потребуется точная работа типографики, чтобы bar не выглядел обычной служебной строкой.</p></div>
          </div>
        </article>

        <article class="section-card decision-lab__variant decision-variant-v3-split-band" data-variant="split-band-free">
          <div class="section-card__inner">
            <div class="decision-lab__variant-head">
              <p class="eyebrow">V3-C · date + admission split · free state</p>
              <h2>Split Date / Free Band</h2>
              <p>Проверяет исходную идею «рядом с компактной датой разместить что-то полезное»: справа не декоративный текст, а обязательный статус «Бесплатно».</p>
            </div>
            <div class="decision-phone">
              <div class="decision-hero decision-hero--night"><span>parallax hero image</span></div>
              <section class="decision-sheet">
                <p class="decision-eyebrow"><span>{sample.type}</span></p>
                <div class="split-decision-band">
                  <time datetime={sample.datetime}><b>{sample.day}</b><span>{sample.month}<small>{sample.weekdayShort} · {sample.time}</small></span></time>
                  <strong class="free-panel"><span>вход</span>Бесплатно</strong>
                </div>
                <h3>{sample.title}</h3>
                <p class="decision-place"><Icon name="pin" /><span><strong>{sample.venue}</strong><small>{sample.address}</small></span></p>
                <a class="decision-primary decision-primary--free" href="#review"><Icon name="ticket" /><span>Зарегистрироваться</span></a>
                <div class="decision-metrics action-metric-row" aria-label="Действия и показатели события">
                  {metrics.map((metric) => <button type="button" aria-label={metric.label} title={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                </div>
              </section>
              <div class="decision-medallions large-medallion-strip"><MedallionStrip items={medallions} mode="large" /></div>
            </div>
            <div class="decision-lab__variant-notes"><p><strong>Плюс:</strong> одна полоса отвечает сразу на два главных вопроса — когда и сколько стоит.</p><p><strong>Риск:</strong> для длинного диапазона цен правая половина тесна; paid-вариант должен показывать короткое «от 600 ₽», а полный диапазон — у CTA.</p></div>
          </div>
        </article>

        <article class="section-card decision-lab__variant decision-variant-v3-ticket-cluster" data-variant="ticket-cluster">
          <div class="section-card__inner">
            <div class="decision-lab__variant-head">
              <p class="eyebrow">V3-D · transaction cluster · price-first</p>
              <h2>Ticket Cluster</h2>
              <p>Самый спокойный коммерческий вариант: компактная дата остаётся отдельной строкой, цена и покупка объединены в один транзакционный блок, а bare icon-row получает персональную подсказку только для ещё не освоенного действия.</p>
            </div>
            <div class="decision-phone">
              <div class="decision-hero decision-hero--paper"><span>parallax hero image</span></div>
              <section class="decision-sheet">
                <p class="decision-eyebrow"><span>{sample.type}</span><time datetime={sample.datetime}><b>{sample.day}</b> {sample.month} · {sample.weekdayShort} · {sample.time}</time></p>
                <h3>{sample.title}</h3>
                <p class="decision-place"><Icon name="pin" /><span><strong>{sample.venue}</strong><small>{sample.address}</small></span></p>
                <div class="ticket-cluster">
                  <p><span>стоимость</span><strong>{sample.price}</strong></p>
                  <a href="#review"><Icon name="ticket" /><span>{sample.primaryCta}</span></a>
                </div>
                <div class="decision-action-onboarding" data-onboarding-user-state="calendar-unused">
                  <aside id="d-calendar-onboarding" class="action-coachmark" role="note" data-onboarding-for="calendar">
                    <button type="button" class="action-coachmark__close" data-onboarding-dismiss aria-label="Закрыть подсказку">×</button>
                    <strong>Сохраните событие себе</strong>
                    <span>.ics, «Мои события» и письма об изменениях — одним действием.</span>
                  </aside>
                  <div class="decision-metrics decision-metrics--bare action-metric-row" aria-label="Действия и показатели события">
                    {metrics.map((metric) => <button type="button" class={metric.icon === 'calendar' ? 'is-onboarding-target' : undefined} aria-label={metric.label} aria-describedby={metric.icon === 'calendar' ? 'd-calendar-onboarding' : undefined} title={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                  </div>
                </div>
              </section>
              <div class="decision-medallions large-medallion-strip"><MedallionStrip items={medallions} mode="large" /></div>
            </div>
            <div class="decision-lab__variant-notes"><p><strong>Плюс:</strong> цена и действие воспринимаются как одна задача, визуальных тяжёлых блоков только два — H1 и ticket cluster.</p><p><strong>Риск:</strong> дата заметно слабее A/B/C; этот вариант допустим только если в тесте пользователи всё равно мгновенно находят «когда».</p></div>
          </div>
        </article>

        <article class="section-card decision-lab__variant decision-variant-v2-date-card decision-variant-v3-control" data-variant="v2-control">
          <div class="section-card__inner">
            <div class="decision-lab__variant-head">
              <p class="eyebrow">Control · preserved from 63e88020</p>
              <h2>V2 Full-width Date Card</h2>
              <p>Сохранённый неплохой вариант из предыдущей итерации. Он нужен как честный контроль: сильнее всех подчёркивает дату, но расходует больше высоты и оставляет длинные подписи действий.</p>
            </div>
            <div class="decision-phone">
              <div class="decision-hero decision-hero--amber"><span>parallax hero image</span></div>
              <section class="decision-sheet">
                <p class="decision-eyebrow"><span>{sample.type}</span><strong class="legacy-admission">по билетам</strong></p>
                <h3>{sample.title}</h3>
                <time class="legacy-date-card" datetime={sample.datetime}>
                  <b>{sample.day}</b><span>{sample.month}</span><strong><em>{sample.weekday}</em>{sample.time}</strong>
                </time>
                <p class="decision-place decision-place--legacy"><Icon name="pin" /><span><strong>{sample.venue}</strong><small>Калининград</small></span></p>
                <a class="decision-primary" href="#review"><Icon name="ticket" /><span>{sample.primaryCta}</span></a>
                <div class="legacy-actions mandatory-action-row">
                  <button type="button"><Icon name="calendar" /><span>Календарь</span></button>
                  <button type="button"><Icon name="share" /><span>Поделиться</span></button>
                  <button type="button"><Icon name="heart" /><span>Нравится</span></button>
                </div>
              </section>
              <div class="decision-medallions large-medallion-strip"><MedallionStrip items={medallions} mode="large" /></div>
            </div>
          </div>
        </article>
      </section>

      <section class="section-card decision-lab__onboarding" data-action-onboarding-lab data-onboarding-policy="one-hint-per-session">
        <div class="section-card__inner">
          <div class="decision-lab__section-head">
            <p class="eyebrow">Pinterest onboarding pass · 80 reviewed · one hint at a time</p>
            <h2>Как обучить icon-only действия в варианте D</h2>
            <p>Подсказка появляется только после короткой задержки, когда action-row уже виден, и привязана к одному действию. Полноэкранный tour, затемнение страницы и одновременные три подсказки запрещены.</p>
          </div>

          <div class="onboarding-pattern-grid" aria-label="Варианты персонального onboarding действий">
            <article class="onboarding-pattern onboarding-pattern-coachmark">
              <p class="onboarding-persona"><span>O1 · рекомендуемый</span><strong>Новый пользователь · календарь ещё не использовал</strong></p>
              <div class="onboarding-demo-surface">
                <aside class="action-coachmark action-coachmark--demo" role="note" data-onboarding-for="calendar">
                  <button type="button" class="action-coachmark__close" data-onboarding-dismiss aria-label="Закрыть подсказку">×</button>
                  <strong>Сохраните событие себе</strong>
                  <span>.ics, «Мои события» и письма об изменениях — одним действием.</span>
                </aside>
                <div class="decision-metrics decision-metrics--bare action-metric-row">
                  {metrics.map((metric) => <button type="button" class={metric.icon === 'calendar' ? 'is-onboarding-target' : undefined} aria-label={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                </div>
              </div>
              <p><strong>Почему:</strong> лучше всего связывает пользу с конкретной иконкой и оставляет всю страницу доступной.</p>
            </article>

            <article class="onboarding-pattern onboarding-pattern-inline">
              <p class="onboarding-persona"><span>O2 · спокойный</span><strong>Календарь освоен · лайк ещё не использовался</strong></p>
              <div class="onboarding-demo-surface">
                <aside class="action-inline-hint" role="note" data-onboarding-for="like">
                  <Icon name="heart" />
                  <span><strong>Нравится?</strong> Лайк улучшает ваши рекомендации и поддерживает событие.</span>
                  <button type="button" data-onboarding-dismiss aria-label="Закрыть подсказку">×</button>
                </aside>
                <div class="decision-metrics decision-metrics--bare action-metric-row">
                  {metrics.map((metric) => <button type="button" class={metric.icon === 'heart' ? 'is-onboarding-target is-onboarding-target--like' : undefined} aria-label={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                </div>
              </div>
              <p><strong>Почему:</strong> вмещает более длинное объяснение, но требует иконки-связки, иначе выглядит общей справкой.</p>
            </article>

            <article class="onboarding-pattern onboarding-pattern-micro">
              <p class="onboarding-persona"><span>O3 · самый тихий</span><strong>Календарь и лайк освоены · share ещё не использовался</strong></p>
              <div class="onboarding-demo-surface onboarding-demo-surface--micro">
                <span class="action-micro-label" data-onboarding-for="share">Отправить друзьям</span>
                <div class="decision-metrics decision-metrics--bare action-metric-row">
                  {metrics.map((metric) => <button type="button" class={metric.icon === 'share' ? 'is-onboarding-target is-onboarding-target--share' : undefined} aria-label={metric.label}><Icon name={metric.icon} /><strong>{metric.value}</strong></button>)}
                </div>
              </div>
              <p><strong>Почему:</strong> почти не добавляет шума. Подходит возвращающемуся пользователю, но недостаточен для сложного calendar-follow.</p>
            </article>
          </div>

          <div class="onboarding-rule-grid" aria-label="Правила персонального выбора подсказки">
            <article><span>1</span><strong>Calendar-first</strong><p>Если нет успешного calendar/follow-действия, обучаем только ему.</p></article>
            <article><span>2</span><strong>Like next</strong><p>Если calendar освоен, но лайка ещё не было, показываем пользу рекомендаций.</p></article>
            <article><span>3</span><strong>Share last</strong><p>Если предыдущие действия освоены, можно один раз подсказать native share.</p></article>
            <article><span>0</span><strong>Ничего</strong><p>Все действия использованы, подсказка закрыта или уже была в этой сессии.</p></article>
          </div>

          <p class="decision-lab__source-note"><strong>Важно про статус:</strong> в текущей V3-ветке calendar-ссылка реально открывает только статический <code>.ics</code>. Сохранение в «Мои события» и email-follow — целевой объединённый контракт; его нельзя обещать на production-странице до интеграции authenticated follow и выхода email worker из dry-run.</p>
        </div>
      </section>

      <section class="section-card decision-lab__research">
        <div class="section-card__inner">
          <div class="decision-lab__section-head"><p class="eyebrow">Research funnel</p><h2>Что пришло из референсов и двух независимых консультаций</h2></div>
          <div class="decision-lab__research-grid">
            <article><h3>Pinterest</h3><p>Собрано и просмотрено 120 кандидатов по 12 направлениям, от event-ticket apps до музейных страниц и icon+count action bars. В shortlist осталось 19. Самые полезные механики: P065 compact date, P061 editorial date, P032/P039 icon metrics.</p></article>
            <article><h3>Gemini 3.1 Pro</h3><p>Предложил sticky CTA, blocked strong date, two-column metadata и metrics header. После критики самым безопасным признал сильную дату с чистым action footer; two-column вариант отклонил для русского текста на 320px.</p></article>
            <article><h3>a-opus</h3><p>Предложил Compact Left Date, Stacked Calm Facts, Date Sidebar и Price-Anchored CTA. Рейтинг: Calm Bar → Compact Tile → Sidebar → Price-first. Отдельно отверг sticky bar и медальоны-аватары.</p></article>
            <article class="decision-lab__recommendation"><h3>Product shortlist · 2026-07-11</h3><p><strong>Продолжаем с V3-A и V3-D.</strong> A сохраняет собственный сильный date tile. D остаётся самым спокойным и теперь получает just-in-time onboarding для bare icon-row. B сохраняется как безопасный fallback, C — как проверка бесплатного события.</p></article>
          </div>
        </div>
      </section>

      <section class="section-card decision-lab__states">
        <div class="section-card__inner">
          <div class="decision-lab__section-head"><p class="eyebrow">Data contract</p><h2>Admission не должен придумывать цену</h2></div>
          <div class="state-grid">
            <article><span>известна цена</span><strong>от 600 ₽ / 600–1 500 ₽</strong><p>Показываем у верхней мета-строки или внутри ticket cluster.</p></article>
            <article><span>бесплатно</span><strong class="state-free">Бесплатно</strong><p>Всегда явно; CTA меняется на регистрацию или получение билета.</p></article>
            <article><span>платно, цена неизвестна</span><strong>без admission-pill</strong><p>Не заменяем на «по билетам». CTA честно говорит «Узнать цену».</p></article>
            <article><span>нет медальонов</span><strong>секция отсутствует</strong><p>Никакой пустой заглушки; следующий блок поднимается к sheet.</p></article>
          </div>
        </div>
      </section>

      <section id="review" class="section-card decision-lab__review">
        <div class="section-card__inner">
          <div class="decision-lab__section-head"><p class="eyebrow">Ручной выбор</p><h2>На что смотреть при сравнении shortlisted A / D</h2></div>
          <ol>
            <li>Находятся ли дата, день недели и время за один короткий скан — без чтения всей карточки?</li>
            <li>Понятно ли без подписей, что icon+number — кликабельные календарь, share и like?</li>
            <li>Считываются ли цена/«Бесплатно» раньше primary CTA и не выглядит ли admission рекламным бейджем?</li>
            <li>Достаточно ли заметны крупные медальоны, не конкурируя с H1 и покупкой?</li>
            <li>Какой вариант спокойнее выглядит на 320px с длинным заголовком и адресом?</li>
          </ol>
          <p class="decision-lab__source-note">Это лаборатория, а не rollout в <code>EventHero.astro</code>. После выбора production-реализация должна сохранить реальную share/calendar/like-логику, аналитику, авторизацию и данные счётчиков.</p>
        </div>
      </section>

      <div hidden aria-hidden="true">{supersededMarkers.join(' · ')} · A/B review · decision-v2-overlap-lab</div>
    </div>
  </main>
</EventLayout>

<style is:global>
  .decision-lab { --ink:#292522; --muted:#70645b; --paper:#fffaf2; --cream:#f2e4d3; --line:rgba(93,65,47,.16); --accent:#b6481d; --deep:#2e2a27; --gold:#e0bd67; --green:#277349; color:var(--ink); }
  .decision-lab__shell { max-width:1180px; }
  .decision-lab .section-card { overflow:hidden; }
  .decision-lab__intro { background:radial-gradient(circle at 14% 8%,rgba(224,189,103,.32),transparent 29%),linear-gradient(145deg,#fff6e8,#fffdf8 58%,#f0dfcb); }
  .decision-lab__intro-inner { display:grid; gap:18px; }
  .decision-lab h1 { max-width:900px; font-size:clamp(2.1rem,6.5vw,4.7rem); line-height:.95; letter-spacing:-.058em; }
  .decision-lab h2 { font-size:clamp(1.45rem,4vw,2.2rem); letter-spacing:-.035em; }
  .decision-lab .lead { max-width:850px; color:var(--muted); font-size:clamp(1.02rem,2.2vw,1.25rem); line-height:1.55; }
  .decision-lab__meta-grid { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:10px; }
  .decision-lab__meta-grid>div,.decision-lab__criteria-grid article,.state-grid article { min-width:0; border:1px solid var(--line); border-radius:22px; background:rgba(255,255,255,.68); padding:14px; }
  .decision-lab__meta-grid span,.state-grid span { display:block; color:var(--muted); font-size:.72rem; letter-spacing:.12em; text-transform:uppercase; }
  .decision-lab__meta-grid code,.decision-lab__meta-grid strong { display:block; margin-top:7px; overflow-wrap:anywhere; font-size:.86rem; }
  .decision-lab__links { display:flex; flex-wrap:wrap; gap:9px; }
  .decision-lab__links a { min-height:44px; display:inline-flex; align-items:center; border:1px solid rgba(182,72,29,.24); border-radius:999px; padding:9px 14px; background:rgba(255,255,255,.68); color:var(--accent); text-decoration:none; font-weight:780; }
  .decision-lab__section-head { max-width:780px; margin-bottom:18px; }
  .decision-lab__section-head p { color:var(--muted); line-height:1.5; }
  .decision-lab__criteria-grid { display:grid; grid-template-columns:repeat(5,minmax(0,1fr)); gap:10px; }
  .decision-lab__criteria-grid h3,.decision-lab__research-grid h3 { margin:0 0 8px; font-size:1rem; }
  .decision-lab__criteria-grid p,.decision-lab__research-grid p { margin:0; color:var(--muted); line-height:1.5; }
  .decision-lab__variants { display:grid; gap:22px; margin:22px 0; }
  .decision-lab__variant-head { max-width:780px; display:grid; gap:7px; margin-bottom:16px; }
  .decision-lab__variant-head p:last-child { margin:0; color:var(--muted); line-height:1.5; }
  .decision-lab__variant-notes { display:grid; gap:7px; margin-top:15px; padding-top:14px; border-top:1px solid var(--line); color:var(--muted); }
  .decision-lab__variant-notes p { margin:0; }

  .decision-phone { inline-size:min(100%,390px); margin-inline:auto; overflow:hidden; border:1px solid rgba(41,37,34,.14); border-radius:32px; background:#ebdece; box-shadow:0 26px 64px rgba(62,41,27,.15); padding-bottom:14px; }
  .decision-hero { min-height:238px; display:grid; align-content:start; justify-items:end; padding:18px; color:rgba(255,250,242,.84); background:linear-gradient(180deg,transparent,rgba(36,30,27,.62)),radial-gradient(circle at 28% 25%,#d8b86b 0 18%,transparent 34%),linear-gradient(135deg,#735039,#1e1a18); }
  .decision-hero--forest { background:linear-gradient(180deg,transparent,rgba(31,34,29,.64)),radial-gradient(circle at 72% 24%,#7a906d 0 18%,transparent 35%),linear-gradient(135deg,#8f694b,#1e2420); }
  .decision-hero--night { background:linear-gradient(180deg,transparent,rgba(22,20,25,.72)),radial-gradient(circle at 28% 24%,#93613e 0 18%,transparent 36%),linear-gradient(135deg,#43333a,#171418); }
  .decision-hero--paper { background:linear-gradient(180deg,transparent,rgba(51,43,35,.58)),radial-gradient(circle at 70% 24%,#c9a77f 0 20%,transparent 36%),linear-gradient(135deg,#806b57,#28211c); }
  .decision-hero span { border:1px solid rgba(255,250,242,.22); border-radius:999px; background:rgba(39,33,29,.34); padding:8px 11px; font-size:.71rem; letter-spacing:.13em; text-transform:uppercase; }
  .decision-sheet { position:relative; z-index:1; display:grid; gap:13px; margin:-46px 11px 0; padding:20px 16px 16px; border:1px solid rgba(96,63,42,.13); border-radius:28px 28px 24px 24px; background:linear-gradient(180deg,#fffdf8,#fff6ea); box-shadow:0 -8px 18px rgba(33,24,18,.05),0 22px 46px rgba(60,39,27,.15); }
  .decision-sheet::before { content:''; position:absolute; top:-7px; left:50%; transform:translateX(-50%); inline-size:76px; block-size:8px; border-radius:999px; background:linear-gradient(90deg,#cf6a38,#aa3f18); box-shadow:0 5px 15px rgba(182,72,29,.26); }
  .decision-eyebrow { min-width:0; min-height:34px; margin:0; display:flex; justify-content:space-between; align-items:center; gap:10px; color:var(--muted); font-size:.72rem; letter-spacing:.12em; text-transform:uppercase; }
  .decision-eyebrow>span { min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  .decision-eyebrow time { color:var(--accent); font-size:.75rem; font-weight:850; letter-spacing:.03em; text-transform:none; white-space:nowrap; }
  .decision-eyebrow time b { font-size:1.12em; font-weight:930; }
  .admission,.legacy-admission { min-height:34px; display:inline-flex; align-items:center; border-radius:999px; padding:6px 10px; white-space:nowrap; font-size:.78rem; font-weight:880; letter-spacing:.02em; text-transform:none; }
  .admission--price,.legacy-admission { border:1px solid rgba(182,72,29,.18); background:rgba(182,72,29,.08); color:var(--accent); }
  .decision-sheet h3 { margin:0; font-size:clamp(1.58rem,7.4vw,2.16rem); line-height:.98; letter-spacing:-.054em; }
  .compact-date-title { display:grid; grid-template-columns:82px minmax(0,1fr); align-items:stretch; gap:15px; }
  .compact-date-title h3 { align-self:center; }
  .compact-date-tile { min-height:98px; display:grid; grid-template-rows:1fr auto auto; place-items:center; overflow:hidden; border-radius:20px; background:var(--deep); color:#fff9f0; box-shadow:inset 0 0 0 1px rgba(255,255,255,.08); }
  .compact-date-tile b { align-self:end; font-size:3.35rem; line-height:.8; letter-spacing:-.11em; }
  .compact-date-tile span { color:var(--gold); font-size:.9rem; font-weight:860; }
  .compact-date-tile small { width:100%; padding:6px 4px; background:rgba(255,255,255,.07); font-size:.67rem; font-weight:820; text-align:center; }
  .calm-date-bar { min-height:50px; display:grid; grid-template-columns:auto auto 1fr auto; align-items:center; overflow:hidden; border-radius:15px; background:var(--deep); color:#fff9f0; box-shadow:inset 0 0 0 1px rgba(255,255,255,.08); }
  .calm-date-bar b { align-self:stretch; display:grid; place-items:center; min-width:54px; background:#ead8bc; color:#332a24; font-size:1.72rem; letter-spacing:-.07em; }
  .calm-date-bar span { padding-left:11px; font-weight:880; }
  .calm-date-bar em { justify-self:end; color:var(--gold); font-size:.78rem; font-style:normal; font-weight:820; text-transform:uppercase; }
  .calm-date-bar strong { padding:0 13px 0 8px; font-size:1.08rem; }
  .split-decision-band { min-height:88px; display:grid; grid-template-columns:1fr .94fr; overflow:hidden; border-radius:20px; background:var(--deep); color:#fff9f0; }
  .split-decision-band time { display:grid; grid-template-columns:auto 1fr; align-items:center; gap:10px; padding:12px 13px; }
  .split-decision-band time b { font-size:3rem; line-height:.8; letter-spacing:-.1em; }
  .split-decision-band time>span { display:grid; gap:4px; color:var(--gold); font-weight:880; }
  .split-decision-band time small { color:#fff9f0; font-size:.7rem; }
  .free-panel { display:grid; align-content:center; gap:3px; padding:12px; background:#dceade; color:#1f5f3b; font-size:1.04rem; text-align:center; }
  .free-panel span { font-size:.66rem; letter-spacing:.13em; text-transform:uppercase; }
  .decision-place { margin:0; display:flex; align-items:flex-start; gap:9px; color:#50463e; line-height:1.32; }
  .decision-place>.icon { inline-size:19px; block-size:19px; margin-top:2px; flex:0 0 auto; color:var(--accent); }
  .decision-place>span { min-width:0; display:grid; gap:2px; }
  .decision-place strong { font-size:.97rem; }
  .decision-place small { color:#6f6259; font-size:.86rem; font-weight:650; overflow-wrap:anywhere; }
  .decision-primary { min-height:55px; display:inline-flex; align-items:center; justify-content:center; gap:9px; border-radius:999px; background:var(--accent); color:#fffaf2; box-shadow:0 15px 27px rgba(182,72,29,.23); text-decoration:none; font-weight:880; }
  .decision-primary--free { background:var(--green); box-shadow:0 15px 27px rgba(39,115,73,.2); }
  .decision-primary .icon,.ticket-cluster .icon { inline-size:20px; block-size:20px; }
  .decision-metrics { min-height:48px; display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:8px; }
  .decision-metrics button { min-width:0; min-height:48px; display:inline-flex; align-items:center; justify-content:center; gap:8px; border:1px solid rgba(96,63,42,.13); border-radius:16px; background:#f1e2ce; color:#463c35; font:inherit; cursor:pointer; touch-action:manipulation; }
  .decision-metrics button:focus-visible { outline:3px solid rgba(182,72,29,.35); outline-offset:2px; }
  .decision-metrics .icon { inline-size:21px; block-size:21px; flex:0 0 auto; }
  .decision-metrics strong { font-size:.88rem; font-variant-numeric:tabular-nums; }
  .decision-metrics--bare button { background:transparent; border-color:transparent; }
  .decision-action-onboarding { display:grid; gap:8px; }
  .action-coachmark { position:relative; display:grid; gap:3px; padding:12px 44px 12px 14px; border-radius:17px; background:var(--deep); color:#fff9f0; box-shadow:0 12px 28px rgba(40,31,25,.18); font-size:.78rem; line-height:1.35; }
  .action-coachmark::after { content:''; position:absolute; left:calc(16.666% - 6px); bottom:-6px; inline-size:12px; block-size:12px; transform:rotate(45deg); border-radius:2px; background:var(--deep); }
  .action-coachmark strong { color:var(--gold); font-size:.84rem; }
  .action-coachmark span { color:rgba(255,249,240,.82); }
  .action-coachmark__close { position:absolute; top:0; right:0; inline-size:44px; block-size:44px; border:0; background:transparent; color:rgba(255,249,240,.72); font:inherit; font-size:1.15rem; cursor:pointer; }
  .decision-metrics--bare button.is-onboarding-target { background:#fff0d9; border-color:rgba(182,72,29,.18); box-shadow:0 0 0 4px rgba(224,189,103,.2); }
  .decision-metrics--bare button.is-onboarding-target--like { background:#f8e5e4; box-shadow:0 0 0 4px rgba(182,72,29,.12); }
  .decision-metrics--bare button.is-onboarding-target--share { background:#e9eee9; box-shadow:0 0 0 4px rgba(39,115,73,.1); }
  .ticket-cluster { display:grid; grid-template-columns:minmax(0,.9fr) minmax(0,1.35fr); gap:9px; padding:10px; border:1px solid rgba(182,72,29,.14); border-radius:21px; background:rgba(182,72,29,.065); }
  .ticket-cluster p { margin:0; display:grid; align-content:center; gap:2px; padding-left:5px; }
  .ticket-cluster p span { color:var(--muted); font-size:.66rem; letter-spacing:.1em; text-transform:uppercase; }
  .ticket-cluster p strong { color:var(--accent); font-size:1.04rem; white-space:nowrap; }
  .ticket-cluster a { min-height:50px; display:inline-flex; align-items:center; justify-content:center; gap:7px; border-radius:15px; background:var(--accent); color:white; text-decoration:none; font-weight:870; }
  .decision-medallions { margin-top:11px; padding-left:12px; }
  .large-medallion-strip .medallion-strip { display:flex; flex-wrap:nowrap; gap:12px; overflow-x:auto; overscroll-behavior-x:contain; scroll-snap-type:x proximity; padding:4px 28px 14px 0; scrollbar-width:thin; }
  .large-medallion-strip .medallion { inline-size:clamp(90px,25vw,108px); block-size:clamp(90px,25vw,108px); flex:0 0 auto; display:grid; place-items:center; overflow:hidden; border:3px solid rgba(255,253,248,.9); border-radius:50%; background:#fff; box-shadow:0 12px 28px rgba(54,38,26,.18); scroll-snap-align:start; }
  .large-medallion-strip .medallion img { width:100%; height:100%; object-fit:cover; }
  .large-medallion-strip .medallion span { color:var(--accent); font-size:.8rem; font-weight:920; }
  .legacy-date-card { min-height:100px; display:grid; grid-template-columns:.72fr .62fr .9fr; overflow:hidden; border-radius:23px; background:var(--deep); color:#fff9f0; }
  .legacy-date-card>b { display:grid; place-items:center; font-size:4.75rem; line-height:.78; letter-spacing:-.11em; }
  .legacy-date-card>span { display:grid; place-items:center; background:#ead8bc; color:#332a24; font-size:1.2rem; font-weight:870; }
  .legacy-date-card>strong { display:grid; align-content:center; gap:7px; padding:10px; font-size:1.35rem; }
  .legacy-date-card em { color:var(--gold); font-size:.8rem; font-style:normal; letter-spacing:.08em; text-transform:uppercase; }
  .legacy-actions { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:6px; }
  .legacy-actions button { min-width:0; min-height:48px; display:inline-flex; align-items:center; justify-content:center; gap:5px; border:1px solid rgba(96,63,42,.13); border-radius:999px; background:#f1e2ce; color:#463c35; font:inherit; font-size:.7rem; font-weight:820; }
  .legacy-actions .icon { inline-size:18px; block-size:18px; flex:0 0 auto; }

  .decision-lab__onboarding { background:radial-gradient(circle at 88% 4%,rgba(224,189,103,.24),transparent 28%),linear-gradient(145deg,#fffaf2,#f2e4d3); }
  .onboarding-pattern-grid { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:12px; }
  .onboarding-pattern { min-width:0; display:grid; align-content:start; gap:12px; border:1px solid var(--line); border-radius:24px; background:rgba(255,255,255,.72); padding:14px; }
  .onboarding-pattern>p { margin:0; color:var(--muted); line-height:1.45; }
  .onboarding-persona { display:grid; gap:5px; min-height:66px; }
  .onboarding-persona span { color:var(--accent); font-size:.7rem; font-weight:850; letter-spacing:.1em; text-transform:uppercase; }
  .onboarding-persona strong { color:var(--ink); font-size:.92rem; }
  .onboarding-demo-surface { min-height:174px; display:grid; align-content:end; gap:10px; overflow:hidden; border:1px solid rgba(96,63,42,.11); border-radius:20px; background:linear-gradient(180deg,#fffdf8,#f5eadb); padding:12px; }
  .onboarding-demo-surface .decision-metrics { min-width:0; }
  .onboarding-demo-surface .action-coachmark { font-size:.73rem; }
  .action-inline-hint { display:grid; grid-template-columns:auto minmax(0,1fr) 36px; align-items:center; gap:9px; border:1px solid rgba(182,72,29,.15); border-radius:17px; background:#fff1df; color:#55463c; padding:9px 7px 9px 11px; font-size:.74rem; line-height:1.35; }
  .action-inline-hint>.icon { inline-size:21px; block-size:21px; color:var(--accent); }
  .action-inline-hint span { min-width:0; }
  .action-inline-hint strong { color:var(--ink); }
  .action-inline-hint button { min-width:36px; min-height:44px; border:0; background:transparent; color:var(--muted); font:inherit; font-size:1.05rem; }
  .onboarding-demo-surface--micro { position:relative; padding-top:54px; }
  .action-micro-label { position:absolute; top:20px; left:50%; transform:translateX(-50%); border-radius:999px; background:var(--deep); color:#fff9f0; padding:7px 10px; font-size:.7rem; font-weight:820; white-space:nowrap; box-shadow:0 8px 18px rgba(40,31,25,.15); }
  .action-micro-label::after { content:''; position:absolute; left:50%; bottom:-5px; inline-size:10px; block-size:10px; transform:translateX(-50%) rotate(45deg); border-radius:2px; background:var(--deep); }
  .onboarding-rule-grid { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:10px; margin-top:16px; }
  .onboarding-rule-grid article { min-width:0; display:grid; grid-template-columns:30px minmax(0,1fr); gap:3px 8px; align-items:center; border:1px solid var(--line); border-radius:19px; background:rgba(255,255,255,.62); padding:11px; }
  .onboarding-rule-grid article>span { grid-row:1/3; inline-size:30px; block-size:30px; display:grid; place-items:center; border-radius:50%; background:var(--deep); color:var(--gold); font-weight:900; }
  .onboarding-rule-grid strong { font-size:.86rem; }
  .onboarding-rule-grid p { margin:0; color:var(--muted); font-size:.76rem; line-height:1.35; }

  .decision-lab__research { background:linear-gradient(145deg,#2e2a27,#493b31); color:#fff9f0; }
  .decision-lab__research .eyebrow,.decision-lab__research .decision-lab__section-head p,.decision-lab__research-grid p { color:rgba(255,249,240,.72); }
  .decision-lab__research-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:11px; }
  .decision-lab__research-grid article { border:1px solid rgba(255,249,240,.15); border-radius:22px; background:rgba(255,255,255,.055); padding:16px; }
  .decision-lab__research-grid h3 { color:#fff9f0; }
  .decision-lab__recommendation { background:linear-gradient(145deg,rgba(182,72,29,.42),rgba(224,189,103,.16))!important; }
  .state-grid { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:10px; }
  .state-grid strong { display:block; margin:8px 0; font-size:1.06rem; }
  .state-grid p { margin:0; color:var(--muted); line-height:1.45; }
  .state-grid .state-free { color:var(--green); }
  .decision-lab__review ol { display:grid; gap:9px; padding-left:1.2rem; color:var(--muted); line-height:1.55; }
  .decision-lab__source-note { max-width:850px; color:var(--muted); line-height:1.55; }

  @media (min-width:820px) { .decision-lab__variants--v3 { grid-template-columns:repeat(2,minmax(0,1fr)); } }
  @media (max-width:980px) { .decision-lab__criteria-grid,.onboarding-pattern-grid { grid-template-columns:repeat(2,minmax(0,1fr)); } .state-grid,.onboarding-rule-grid { grid-template-columns:repeat(2,minmax(0,1fr)); } }
  @media (max-width:620px) {
    .decision-lab__meta-grid,.decision-lab__criteria-grid,.decision-lab__research-grid,.state-grid,.onboarding-pattern-grid,.onboarding-rule-grid { grid-template-columns:1fr; }
    .decision-lab__variant .section-card__inner { padding-inline:11px; }
    .decision-phone { border-radius:27px; }
    .decision-hero { min-height:224px; }
    .decision-sheet { margin-inline:9px; padding-inline:14px; }
    .large-medallion-strip .medallion { inline-size:clamp(90px,28vw,104px); block-size:clamp(90px,28vw,104px); }
  }
  @media (max-width:350px) {
    .decision-lab h1 { font-size:2rem; }
    .decision-hero { min-height:208px; }
    .decision-sheet { margin-top:-42px; gap:11px; }
    .compact-date-title { grid-template-columns:72px minmax(0,1fr); gap:10px; }
    .compact-date-tile { min-height:90px; border-radius:17px; }
    .compact-date-tile b { font-size:3rem; }
    .decision-sheet h3 { font-size:clamp(1.42rem,8vw,1.75rem); }
    .calm-date-bar b { min-width:48px; font-size:1.52rem; }
    .calm-date-bar span { padding-left:8px; }
    .calm-date-bar strong { padding-right:9px; font-size:.98rem; }
    .split-decision-band { grid-template-columns:1.08fr .92fr; }
    .split-decision-band time { gap:7px; padding-inline:10px; }
    .split-decision-band time b { font-size:2.6rem; }
    .free-panel { padding-inline:8px; font-size:.92rem; }
    .ticket-cluster { grid-template-columns:1fr; }
    .ticket-cluster p { grid-template-columns:auto 1fr; align-items:center; gap:8px; }
    .legacy-date-card { grid-template-columns:1fr 1fr; }
    .legacy-date-card>strong { grid-column:1/-1; grid-template-columns:1fr auto; }
    .legacy-actions { grid-template-columns:1fr; }
  }
</style>

<script>
  document.querySelectorAll('[data-onboarding-dismiss]').forEach((button) => {
    button.addEventListener('click', () => {
      const hint = button.closest('[data-onboarding-for]');
      if (!hint) return;
      hint.hidden = true;
      hint.parentElement?.querySelector('.is-onboarding-target')?.classList.remove('is-onboarding-target', 'is-onboarding-target--like', 'is-onboarding-target--share');
    });
  });
  // Static preview markers: event-decision-lab decision-v3-overlap-lab decision-variant-v3-compact-tile decision-variant-v3-calm-bar decision-variant-v3-split-band decision-variant-v3-ticket-cluster action-metric-row action-onboarding-lab onboarding-pattern-coachmark onboarding-pattern-inline onboarding-pattern-micro one-hint-per-session large-medallion-strip baseline preserved A/B review.
</script>
