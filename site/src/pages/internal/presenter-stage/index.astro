---
import { withBase } from '../../../lib/events';

const homeUrl = withBase('/');
const weekendUrl = withBase('/vyhodnye/');
const focusPreviewBase = 'https://kenigevents.ru/preview-20260729-focus-simple-r15-a5cc0256';
const focusInvitationUrl = `${focusPreviewBase}/fokus-gruppa/priglashenie/#invite=focus-group-2026-announcements`;
const focusSearchUrl = `${focusPreviewBase}/poisk/`;
const focusMedallionUrl = `${focusPreviewBase}/sobytiya/otkrytie-festivalya-bahosluzhenie-proekt-evgeniya-knyazeva-ya-gospodi-tvor-kaliningrad-6153/`;
const outroQrUrl = 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/qr-survey-nikiforov-916b6fee58256c4f2111887bf70c502070a55e45a667650dfccdb1495016ccd9.png';
const znanieLogoUrl = 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/znanie-logo-b97bf38f1b152a8eb3bbae79cb38df24cc2543ec2538d6f0d58863c9698072a9.svg';
const introMusicUrl = 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/echo-sax-end-6d7494c0d24c1815ac72a120e96b23782a2e92ef1ce26fb67769693d057fd08a.mp3';
const announcementsWordmarkUrl = '/brand/announcements-wordmark-ui.svg';
const shareIconUrl = '/assets/icons/reference4/share-nodes.svg';
const celebrityAvatarUrl = '/assets/participants/udovenko-tatyana.webp';
const lectureSlides = [
  {
    messageId: 821,
    chapter: '01 · UI ≠ UX',
    statement: 'Интерфейс видят. Опыт проживают.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-821-2f3c1b7d9a1c7094c77da009867c25a62cb233110185ab7a1a020b61356bdc26.webp',
    alt: 'Сравнение пользовательского интерфейса и опыта на примере игрушки над детской кроваткой',
    fit: 'contain',
    theme: 'dark',
    layout: 'split',
    caption: 'Красивый объект ещё не гарантирует хороший опыт.',
  },
  {
    messageId: 822,
    chapter: '02 · Проверка действием',
    statement: 'Удобство проверяется действием.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-822-d85cadfd1dd4aad0c0e8f5fb68482624a1a4d617585fae9737fd430fac9513d1.webp',
    alt: 'Сравнение двух бутылок кетчупа и опыта их использования',
    fit: 'contain',
    theme: 'light',
    layout: 'horizontal',
    caption: 'Проверять удобство нужно не взглядом, а действием.',
  },
  {
    messageId: 823,
    chapter: '03 · Эргономика телефона',
    statement: 'Интерфейс должен быть под рукой.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-823-61de6c5363792bc2fb5e50d6182c90f926fa7021c12d9bd80f96dff5b9d62bdc.webp',
    alt: 'Зоны доступности интерфейса смартфона для большого пальца',
    fit: 'contain',
    theme: 'dark',
    layout: 'poster',
    caption: 'Телефон держат одной рукой. Интерфейс должен это помнить.',
  },
  {
    messageId: 824,
    chapter: '04 · Цена сложности',
    statement: 'Когда всё важно — ничего не понятно.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-824-19c1f686d1b04f0b99463d24a16625827f0ff3e45bccbffb7c6e3b9d8f5d561b.webp',
    alt: 'Иллюстрация перегруженного пульта управления и растерянного оператора',
    fit: 'wide',
    theme: 'light',
    layout: 'horizontal',
    caption: 'Перегрузка интерфейса превращает простое решение в риск.',
  },
  {
    messageId: 825,
    chapter: '05 · Критические системы',
    statement: 'В сложных системах удобство — безопасность.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-825-1734e48aa4ef2b20c19c975c76d91b507fe98999d49655bf71749e0b8fd9f43b.webp',
    alt: 'Обложка книги Вопросы эргономики в ядерной энергетике',
    fit: 'portrait',
    theme: 'dark',
    layout: 'portrait',
    caption: 'Эргономика — часть безопасности критических систем.',
  },
  {
    messageId: 826,
    chapter: '06 · Видеть главное',
    statement: 'Хорошая система помогает видеть главное.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-826-252137528a929f8165c441b6fccdc853169fd4c4a249c0870f3b9b26a15ecadb.webp',
    alt: 'Организованный большой зал управления с операторами и обзорными экранами',
    fit: 'contain',
    theme: 'light',
    layout: 'cinema',
    caption: 'Хорошая система помогает быстро увидеть отклонение.',
  },
  {
    messageId: 830,
    chapter: '07 · Работа над удобством',
    statement: 'Понять → спроектировать → проверить.',
    image: 'https://static.kenigevents.ru/assets/autopresenter/scenario-20260730/lecture-830-cdcb0a381189715a8cebf90122000b7ddad74350390df87fd881fed658eff582.webp',
    alt: 'Цикл пользовательского проектирования от анализа к дизайну и тестированию',
    fit: 'contain',
    theme: 'dark',
    layout: 'split',
    caption: 'Удобство рождается из наблюдения, проектирования и проверки.',
  },
];
const serviceTextScenes = [
  { id: 'service-disruption', eyebrow: '07 · Честный момент', title: 'И тут презентация\nпошла не по плану…', theme: 'dark' },
  { id: 'service-taste', eyebrow: '08 · Не только свой вкус', title: 'Удобство нельзя\nсделать для себя.', support: 'Сервисом будут пользоваться другие люди.', theme: 'light' },
  { id: 'service-feedback', eyebrow: '09 · Слушать аудиторию', title: 'Обратная связь —\nчасть интерфейса.', support: 'Не приложение для людей. Приложение вместе с людьми.', theme: 'dark' },
  { id: 'service-nps', eyebrow: '11 · Простая проверка', title: 'Посоветовали бы\nэту страницу?', support: 'NPS помогает измерять не красоту, а готовность рекомендовать.', theme: 'light' },
];
---
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow, noarchive" />
    <meta name="color-scheme" content="dark" />
    <link rel="preload" as="image" href={outroQrUrl} fetchpriority="high" />
    <link rel="preload" as="image" href={znanieLogoUrl} fetchpriority="high" />
    <link rel="preload" as="audio" href={introMusicUrl} />
    {lectureSlides.map((slide) => <link rel="preload" as="image" href={slide.image} />)}
    <title>Автопрезентатор · Сцена</title>
  </head>
  <body>
    <main
      class="stage"
      data-presenter-id="presenter-stage"
      data-presenter-stage-ready="true"
      data-presenter-scene="live-site"
      aria-label="Сцена автопрезентатора"
    >
      <div
        class="live-site-scene"
        data-presenter-id="live-site-scene"
        data-presenter-scene-id="live-site"
      >
        <p class="stage-caption" aria-hidden="true">Завтра — в одном движении.</p>
        <section class="device-area" aria-label="Мобильный сайт">
          <div class="ambient ambient-one" aria-hidden="true"></div>
          <div class="ambient ambient-two" aria-hidden="true"></div>
          <div class="phone-shell">
            <div class="phone-top" aria-hidden="true"><span class="speaker"></span><span class="camera"></span></div>
            <iframe
              id="presenter-mobile-frame"
              data-presenter-id="mobile-site-frame"
              data-presenter-frame-ready="false"
              src={homeUrl}
              title="Мобильная версия сайта Кёнигсберг · События"
              width="476"
              height="1024"
            ></iframe>
            <div class="phone-home" aria-hidden="true"></div>
          </div>
        </section>
      </div>

      <section
        class="intro-scene presentation-scene"
        data-presenter-id="intro-scene"
        data-presenter-scene-id="intro-loop"
        data-intro-state="idle"
        aria-label="Интро перед лекцией"
        hidden
      >
        <div class="presentation-orbit" aria-hidden="true"></div>
        <div class="brand-plate brand-plate--intro brand-logo--on-dark">
          <img src={znanieLogoUrl} alt="Знание" width="540" height="125" loading="eager" decoding="sync" />
        </div>
        <p class="intro-kicker">80 историй о главном · Калининград</p>
        <h1 class="intro-thought" data-presenter-id="intro-thought" aria-live="off">
          <span data-intro-line="one" aria-hidden="true"></span>
          <span data-intro-line="two" aria-hidden="true"></span>
        </h1>
        <p class="intro-footnote">До начала можно знакомиться и общаться</p>
        <audio
          data-presenter-id="intro-music"
          src={introMusicUrl}
          preload="auto"
          loop
        ></audio>
      </section>

      {lectureSlides.map((slide, index) => (
        <section
          class={`lecture-slide presentation-scene lecture-slide--${slide.fit} lecture-layout--${slide.layout} theme--${slide.theme}`}
          data-presenter-id="lecture-scene"
          data-presenter-scene-id={`lecture-${String(index + 1).padStart(2, '0')}`}
          data-lecture-index={index}
          data-source-message-id={slide.messageId}
          aria-label={`Лекционный экран ${index + 1}`}
          hidden
        >
            <div class="lecture-copy">
              <div class={`brand-plate brand-plate--lecture brand-logo--on-${slide.theme}`}>
                <img src={znanieLogoUrl} alt="Знание" width="540" height="125" loading="eager" decoding="sync" />
              </div>
              <p class="lecture-chapter">{slide.chapter}</p>
              <h2>{slide.statement}</h2>
              <p class="lecture-counter">{String(index + 1).padStart(2, '0')} / {String(lectureSlides.length).padStart(2, '0')}</p>
            </div>
            <figure class="lecture-visual">
              <img src={slide.image} alt={slide.alt} loading="eager" decoding="async" />
              <figcaption>{slide.caption}</figcaption>
            </figure>
        </section>
      ))}

      <section class="service-scene service-wordmark presentation-scene theme--light" data-presenter-scene-id="service-wordmark" hidden>
        <p class="service-eyebrow">01 · Полюбить Калининград</p>
        <div class="wordmark-motion" role="img" aria-label="Анонсы">
          <span class="wordmark-plain" aria-hidden="true">Анонсы</span>
          <img class="wordmark-vector" src={announcementsWordmarkUrl} alt="" aria-hidden="true" />
          <svg class="wordmark-o-layer" viewBox="0 0 7819 1514" aria-hidden="true">
            <path class="wordmark-o-glyph" fill="currentColor" fill-rule="evenodd" d="M3371 410C3971 410 4171 540 4171 962C4171 1384 3971 1514 3371 1514C2771 1514 2571 1384 2571 962C2571 540 2771 410 3371 410ZM3371 600C2981 600 2831 700 2831 962C2831 1224 2981 1324 3371 1324C3761 1324 3911 1224 3911 962C3911 700 3761 600 3371 600Z" />
          </svg>
        </div>
      </section>

      <section class="service-scene service-needs presentation-scene theme--dark" data-presenter-scene-id="service-needs" hidden>
        <p class="service-eyebrow">02 · Что нужно человеку</p>
        <h2>Найти. Поделиться.<br />Не забыть.</h2>
        <div class="need-row">
          <div><strong>01</strong><span>Быстро найти<br />то, что нравится</span></div>
          <div><img src={shareIconUrl} alt="" /><span>Позвать<br />друзей</span></div>
          <div>
            <svg viewBox="0 0 64 64" role="img" aria-label="Календарь"><rect x="8" y="13" width="48" height="43" rx="10"/><path d="M8 27h48M21 8v12M43 8v12"/><circle cx="32" cy="41" r="5"/></svg>
            <span>Добавить<br />в календарь</span>
          </div>
        </div>
      </section>

      <section class="service-scene presentation-scene theme--light" data-presenter-scene-id="service-medallions" hidden>
        <p class="service-eyebrow">04 · Смысл одним взглядом</p>
        <h2>Медальон отвечает<br />раньше текста.</h2>
        <div class="medallion-demo" aria-label="Примеры медальонов">
          <span>Бесплатно</span><span>Третьяковка</span><span>Для всей семьи</span>
        </div>
        <p class="service-support">Где? Для кого? На каких условиях? Ключевой факт виден сразу.</p>
      </section>

      <section class="focus-live-scene desktop-medallion-scene presentation-scene theme--light" data-presenter-scene-id="service-medallions-desktop" data-focus-phase="meaning" hidden>
        <aside class="live-meaning live-meaning--cover light"><p>Реальная страница</p><h2>Сначала —<br />главное.</h2><span>Медальоны находятся рядом с событием, а не прячутся в описании.</span></aside>
        <iframe data-presenter-id="medallion-desktop-frame" data-src={focusMedallionUrl} title="Реальная страница события с медальонами" width="1360" height="1080"></iframe>
      </section>

      <section class="focus-live-scene mobile-medallion-scene presentation-scene theme--dark" data-presenter-scene-id="service-medallions-mobile" hidden>
        <aside class="live-meaning"><p>Тот же смысл</p><h2>Теперь —<br />в ладони.</h2><span>Телефон сдвигается, чтобы оставить место мысли.</span></aside>
        <div class="phone-shell focus-phone">
          <div class="phone-top" aria-hidden="true"><span class="speaker"></span><span class="camera"></span></div>
          <iframe data-presenter-id="medallion-mobile-frame" data-src={focusMedallionUrl} title="Мобильная страница события с медальонами" width="476" height="1024"></iframe>
          <div class="phone-home" aria-hidden="true"></div>
        </div>
      </section>

      <section class="service-scene service-joke presentation-scene theme--dark" data-presenter-scene-id="service-joke" data-joke-state="idle" hidden>
        <p class="service-eyebrow">05 · Нужна пауза</p>
        <h2 data-joke-line="one"></h2>
        <p data-joke-line="two"></p>
      </section>

      <section class="service-scene presentation-scene theme--light" data-presenter-scene-id="service-search-concept" hidden>
        <p class="service-eyebrow">06 · Умный поиск</p>
        <h2>Не совпадение слов.<br />Совпадение смыслов.</h2>
        <div class="search-example"><span>«хочу тихий вечер с музыкой»</span><i>→</i><strong>камерный концерт</strong></div>
      </section>

      <section class="focus-live-scene search-live-scene presentation-scene theme--light" data-presenter-scene-id="service-search-live" hidden>
        <aside class="live-meaning light"><p>Живой поиск</p><h2 data-search-meaning>Скажите,<br />чего хочется.</h2><span>Запрос приходит с пульта и печатается на телефоне.</span></aside>
        <div class="phone-shell focus-phone">
          <div class="phone-top" aria-hidden="true"><span class="speaker"></span><span class="camera"></span></div>
          <iframe data-presenter-id="focus-search-frame" data-src={focusSearchUrl} title="Умный поиск фокус-версии сайта" width="476" height="1024"></iframe>
          <div class="phone-home" aria-hidden="true"></div>
        </div>
      </section>

      {serviceTextScenes.map((scene) => (
        <section class={`service-scene presentation-scene theme--${scene.theme}`} data-presenter-scene-id={scene.id} hidden>
          <p class="service-eyebrow">{scene.eyebrow}</p>
          <h2>{scene.title.split('\n').map((line) => <span>{line}</span>)}</h2>
          {scene.support && <p class="service-support">{scene.support}</p>}
        </section>
      ))}

      <section class="focus-live-scene invitation-scene presentation-scene theme--dark" data-presenter-scene-id="service-focus-group" hidden>
        <aside class="live-meaning"><p>10 · Фокус-группа</p><h2>Удобство<br />делают вместе.</h2><span>Показываем именно версию сайта для фокус-группы.</span></aside>
        <iframe data-presenter-id="focus-invitation-frame" data-src={focusInvitationUrl} title="Приглашение в фокус-группу" width="1120" height="1080"></iframe>
      </section>

      <section class="service-scene celebrity-scene presentation-scene theme--dark" data-presenter-scene-id="service-future-celebrity" hidden>
        <p class="service-eyebrow">12 · Скоро</p>
        <h2>Люди, за которыми<br />хочется следить.</h2>
        <div class="celebrity">
          <img src={celebrityAvatarUrl} alt="Татьяна Удовенко" />
          <div><strong>Татьяна Удовенко</strong><span>актриса · режиссёр</span></div>
          <button type="button" aria-label="Нравится Татьяна Удовенко"><b>♥</b><span>128</span></button>
        </div>
      </section>

      <section
        class="desktop-scene"
        data-presenter-id="desktop-scene"
        data-presenter-scene-id="weekend-desktop"
        aria-label="Десктопная страница событий на выходные"
        hidden
      >
        <div class="desktop-meaning" data-presenter-id="desktop-meaning">
          <p>Выходные · FHD</p>
          <h2>Весь город.<br />На одном экране.</h2>
          <span>Сначала мысль. Затем — живой сайт без подписи рядом.</span>
        </div>
        <iframe
          id="presenter-desktop-frame"
          data-presenter-id="desktop-site-frame"
          data-presenter-frame-ready="false"
          data-src={weekendUrl}
          title="Десктопная версия страницы событий на выходные"
          width="1920"
          height="1080"
        ></iframe>
      </section>

      <section
        class="outro-scene presentation-scene"
        data-presenter-id="outro-scene"
        data-presenter-scene-id="outro-qr"
        aria-labelledby="outro-heading"
        hidden
      >
        <div class="outro-copy">
          <p class="outro-kicker">Ваше впечатление важно</p>
          <h1 id="outro-heading">Как вам?</h1>
          <p class="outro-support">Оцените событие — это займёт минуту.</p>
        </div>
        <div class="outro-qr-wrap">
          <img
            class="outro-qr"
            data-presenter-id="outro-qr-image"
            src={outroQrUrl}
            alt="QR-код опроса по итогам мероприятия"
            width="1155"
            height="1155"
            loading="eager"
            decoding="sync"
            fetchpriority="high"
          />
        </div>
      </section>

      <div class="presenter-signals" data-presenter-id="stage-status-card">
        <strong data-presenter-id="stage-status" data-status="idle" role="status" aria-live="polite">Готов к запуску</strong>
      </div>
    </main>

    <script is:inline>
      (() => {
        const stage = document.querySelector('[data-presenter-id="presenter-stage"]');
        const mobileFrame = document.querySelector('#presenter-mobile-frame');
        const desktopFrame = document.querySelector('#presenter-desktop-frame');
        const status = document.querySelector('[data-presenter-id="stage-status"]');
        const stateCard = document.querySelector('[data-presenter-id="stage-status-card"]');
        const introScene = document.querySelector('[data-presenter-scene-id="intro-loop"]');
        const outroScene = document.querySelector('[data-presenter-scene-id="outro-qr"]');
        const introLineOne = document.querySelector('[data-intro-line="one"]');
        const introLineTwo = document.querySelector('[data-intro-line="two"]');
        const introMusic = document.querySelector('[data-presenter-id="intro-music"]');
        const scenes = new Map(
          [...document.querySelectorAll('[data-presenter-scene-id]')]
            .filter((node) => node instanceof HTMLElement)
            .map((node) => [node.dataset.presenterSceneId, node]),
        );
        if (
          !(stage instanceof HTMLElement) ||
          !(mobileFrame instanceof HTMLIFrameElement) ||
          !(desktopFrame instanceof HTMLIFrameElement) ||
          !(status instanceof HTMLElement) ||
          !(introScene instanceof HTMLElement) ||
          !(outroScene instanceof HTMLElement) ||
          !(introLineOne instanceof HTMLElement) ||
          !(introLineTwo instanceof HTMLElement) ||
          !(introMusic instanceof HTMLAudioElement)
        ) return;

        const labels = {
          disconnected: 'Нет связи с агентом',
          idle: 'Готов к запуску',
          running: 'Сценарий выполняется',
          stopping: 'Останавливаем показ',
          completed: 'Показ завершён',
          error: 'Нужна проверка',
        };
        const introPhrases = {
          welcome: ['Добро пожаловать!', 'Скоро начинаем.'],
          topic: ['Почему одни сервисы удобны,', 'а другие — нет?'],
          networking: ['Пока знакомьтесь друг с другом.', 'Сегодня здесь много уважаемых гостей.'],
          heat: ['Сегодня жаркий день.', 'Рады, что вы пришли на презентацию.'],
          format: ['Лекция — и презентация сервиса анонсов.', 'Сегодня вы — наша фокус-группа.'],
        };
        const introRoutes = [
          ['welcome', 'heat', 'networking', 'topic', 'format'],
          ['welcome', 'networking', 'heat', 'topic', 'format'],
          ['welcome', 'topic', 'format', 'heat', 'networking'],
          ['welcome', 'topic', 'format', 'networking', 'heat'],
        ];
        const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)');
        let generation = 0;
        let activeTimer = 0;
        let activeWaitResolve = null;
        let musicFade = 0;
        let previousRoute = -1;

        const setStatus = (nextStatus, label) => {
          if (!Object.hasOwn(labels, nextStatus)) return;
          status.dataset.status = nextStatus;
          status.textContent = label || labels[nextStatus];
          if (stateCard instanceof HTMLElement) stateCard.dataset.status = nextStatus;
        };

        const cancelWait = () => {
          if (activeTimer) clearTimeout(activeTimer);
          activeTimer = 0;
          if (activeWaitResolve) activeWaitResolve(false);
          activeWaitResolve = null;
        };

        const pause = (ms, token) => new Promise((resolve) => {
          cancelWait();
          if (token !== generation) return resolve(false);
          activeWaitResolve = resolve;
          activeTimer = window.setTimeout(() => {
            activeTimer = 0;
            activeWaitResolve = null;
            resolve(token === generation);
          }, ms);
        });

        const stopPresentationMotion = ({ rewindAudio = true } = {}) => {
          generation += 1;
          cancelWait();
          if (musicFade) clearInterval(musicFade);
          musicFade = 0;
          introMusic.pause();
          introMusic.volume = 0;
          if (rewindAudio) {
            try { introMusic.currentTime = 0; } catch {}
          }
          introScene.dataset.introState = 'stopped';
          document.querySelector('[data-joke-state]')?.setAttribute('data-joke-state', 'stopped');
        };

        const splitSemanticChunks = (value) => {
          const words = value.trim().split(/\s+/u);
          const midpoint = Math.ceil(words.length / 2);
          return [words.slice(0, midpoint).join(' '), words.slice(midpoint).join(' ')].filter(Boolean);
        };

        const revealHeroTalk = async (lineOne, lineTwo, token) => {
          introLineOne.replaceChildren();
          introLineTwo.replaceChildren();
          const chunks = [
            ...splitSemanticChunks(lineOne).map((text) => [introLineOne, text]),
            ...splitSemanticChunks(lineTwo).map((text) => [introLineTwo, text]),
          ];
          for (const [node, text] of chunks) {
            if (token !== generation) return false;
            const fragment = document.createElement('span');
            fragment.className = 'hero-fragment is-active';
            fragment.textContent = `${text} `;
            node.append(fragment);
            if (!reduceMotion.matches && !await pause(280, token)) return false;
            fragment.classList.remove('is-active');
            fragment.classList.add('is-complete');
          }
          return token === generation;
        };

        const pickRoute = () => {
          let index = Math.floor(Math.random() * introRoutes.length);
          if (introRoutes.length > 1 && index === previousRoute) index = (index + 1) % introRoutes.length;
          previousRoute = index;
          return introRoutes[index];
        };

        const startMusic = async (token) => {
          introMusic.volume = 0;
          try {
            await introMusic.play();
          } catch {
            introScene.dataset.introAudio = 'blocked';
            return;
          }
          if (token !== generation) return;
          introScene.dataset.introAudio = 'playing';
          const startedAt = performance.now();
          musicFade = window.setInterval(() => {
            if (token !== generation) return clearInterval(musicFade);
            const progress = Math.min(1, (performance.now() - startedAt) / 1500);
            introMusic.volume = .15 * progress;
            if (progress >= 1) {
              clearInterval(musicFade);
              musicFade = 0;
            }
          }, 50);
        };

        const countdownPhrase = (startAt) => {
          const timestamp = Date.parse(startAt || '');
          if (!Number.isFinite(timestamp)) return null;
          const minutes = Math.max(0, Math.ceil((timestamp - Date.now()) / 60000));
          return ['Начнём через', `${minutes} ${minutes === 1 ? 'минуту' : minutes > 1 && minutes < 5 ? 'минуты' : 'минут'}.`];
        };

        const startIntro = async (options = {}) => {
          const token = generation;
          introScene.dataset.introState = 'running';
          introScene.dataset.introAudio = 'starting';
          void startMusic(token);
          while (token === generation) {
            const route = pickRoute();
            if (countdownPhrase(options.startAt)) route.splice(1, 0, 'countdown');
            for (const phraseId of route) {
              if (token !== generation) return;
              const phrase = phraseId === 'countdown' ? countdownPhrase(options.startAt) : introPhrases[phraseId];
              if (!phrase) continue;
              introScene.dataset.introPhrase = phraseId;
              introScene.classList.remove('is-clearing');
              if (!await revealHeroTalk(phrase[0], phrase[1], token)) return;
              if (!await pause(5200, token)) return;
              introScene.classList.add('is-clearing');
              if (!await pause(420, token)) return;
              introLineOne.replaceChildren();
              introLineTwo.replaceChildren();
              introScene.classList.remove('is-clearing');
              if (!await pause(900 + Math.random() * 450, token)) return;
            }
            if (!await pause(1400, token)) return;
          }
        };

        const startJoke = async () => {
          const token = generation;
          const scene = document.querySelector('[data-presenter-scene-id="service-joke"]');
          const first = scene?.querySelector('[data-joke-line="one"]');
          const second = scene?.querySelector('[data-joke-line="two"]');
          if (!(scene instanceof HTMLElement) || !(first instanceof HTMLElement) || !(second instanceof HTMLElement)) return;
          scene.dataset.jokeState = 'typing';
          first.textContent = '';
          second.textContent = '';
          for (const grapheme of Array.from('Максим, самое время для шутки')) {
            if (token !== generation) return;
            first.textContent += grapheme;
            if (!await pause(/[,.!?]/u.test(grapheme) ? 180 : /\s/u.test(grapheme) ? 35 : 72, token)) return;
          }
          if (!await pause(7000, token)) return;
          second.textContent = 'нет, я сама шутить не умею…';
          scene.dataset.jokeState = 'complete';
        };

        const showScene = (sceneId, options = {}) => {
          if (!scenes.has(sceneId)) return;
          stopPresentationMotion();
          const token = generation;
          stage.dataset.presenterScene = sceneId;
          scenes.forEach((scene, id) => { scene.hidden = id !== sceneId; });
          const activeScene = scenes.get(sceneId);
          activeScene?.classList.remove('is-entering');
          requestAnimationFrame(() => requestAnimationFrame(() => activeScene?.classList.add('is-entering')));
          activeScene?.querySelectorAll('iframe[data-src]').forEach((frame) => {
            if (
              frame instanceof HTMLIFrameElement
              && (!frame.hasAttribute('src') || frame.getAttribute('src') === '')
            ) frame.src = frame.dataset.src || '';
          });
          outroScene.classList.remove('is-entering');
          if (sceneId === 'outro-qr') {
            requestAnimationFrame(() => requestAnimationFrame(() => outroScene.classList.add('is-entering')));
          }
          if (sceneId === 'intro-loop') void startIntro(options);
          if (sceneId === 'service-joke') void startJoke();
          if (sceneId === 'service-search-live') {
            const meaning = activeScene?.querySelector('[data-search-meaning]');
            if (meaning && options.query) meaning.textContent = `«${options.query}»`;
          }
          if (sceneId === 'service-medallions-desktop') {
            activeScene?.setAttribute('data-focus-phase', 'meaning');
            window.setTimeout(() => {
              if (generation === token) activeScene?.setAttribute('data-focus-phase', 'site');
            }, 4200);
          }
          if (
            sceneId === 'weekend-desktop'
            && (!desktopFrame.hasAttribute('src') || desktopFrame.getAttribute('src') === '')
          ) {
            desktopFrame.dataset.presenterFrameReady = 'false';
            desktopFrame.src = desktopFrame.dataset.src || '';
          }
          if (sceneId === 'weekend-desktop') {
            activeScene?.setAttribute('data-desktop-phase', 'meaning');
            window.setTimeout(() => {
              if (generation === token) activeScene?.setAttribute('data-desktop-phase', 'site');
            }, 4200);
          }
        };

        const requestedTarget = new URLSearchParams(window.location.search).get('target');
        if (requestedTarget) {
          try {
            const target = new URL(requestedTarget, window.location.origin);
            if (target.origin === window.location.origin) mobileFrame.src = target.href;
          } catch {
            setStatus('error', 'Некорректный адрес показа');
          }
        }

        mobileFrame.addEventListener('load', () => {
          mobileFrame.dataset.presenterFrameReady = 'true';
          document.documentElement.dataset.presenterFrameReady = 'true';
        });
        desktopFrame.addEventListener('load', () => {
          desktopFrame.dataset.presenterFrameReady = 'true';
          document.documentElement.dataset.presenterDesktopFrameReady = 'true';
        });

        window.addEventListener('presenter:status', (event) => {
          const detail = event instanceof CustomEvent ? event.detail : null;
          if (detail && typeof detail.status === 'string') setStatus(detail.status, detail.label);
        });
        window.addEventListener('presenter:scene', (event) => {
          const detail = event instanceof CustomEvent ? event.detail : null;
          if (detail && typeof detail.id === 'string') showScene(detail.id, detail);
        });
        window.addEventListener('presenter:stop', () => stopPresentationMotion({ rewindAudio: true }));

        document.addEventListener('keydown', async (event) => {
          if (event.key.toLowerCase() !== 'f' || event.repeat || event.altKey || event.ctrlKey || event.metaKey) return;
          const target = event.target;
          if (target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement || target instanceof HTMLSelectElement || target?.isContentEditable) return;
          event.preventDefault();
          try {
            if (document.fullscreenElement) await document.exitFullscreen();
            else await document.documentElement.requestFullscreen();
          } catch {
            setStatus('error', 'Полный экран недоступен');
          }
        });
      })();
    </script>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    background: #080a0f;
    --stage-ink: #f7f1e8;
    --stage-accent: #ff7657;
    --stage-violet: #6956df;
  }
  * { box-sizing: border-box; }
  html, body { width: 100vw; height: 100vh; min-width: 320px; margin: 0; overflow: hidden; background: #080a0f; }
  [hidden] { display: none !important; }
  .stage {
    position: relative;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    display: grid;
    place-items: center;
    color: var(--stage-ink);
    background:
      radial-gradient(circle at 50% 44%, rgba(255, 118, 87, .16), transparent 27%),
      radial-gradient(circle at 50% 82%, rgba(105, 86, 223, .11), transparent 29%),
      linear-gradient(135deg, #101116 0%, #080a0f 52%, #101117 100%);
    isolation: isolate;
  }
  .stage::before {
    position: absolute;
    inset: 0;
    z-index: -1;
    content: "";
    opacity: .18;
    background-image: linear-gradient(rgba(255,255,255,.05) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.05) 1px, transparent 1px);
    background-size: 64px 64px;
    mask-image: radial-gradient(circle at center, #000, transparent 67%);
  }
  .live-site-scene { position: absolute; inset: 0; display: grid; place-items: center; }
  .stage-caption {
    position: absolute;
    top: clamp(24px, 5vh, 54px);
    left: clamp(24px, 4vw, 76px);
    max-width: 280px;
    margin: 0;
    color: rgba(247, 241, 232, .68);
    font: 500 clamp(15px, 1.05vw, 20px)/1.35 Georgia, "Times New Roman", serif;
    letter-spacing: .015em;
  }
  .stage-caption::before { display: inline-block; width: 24px; height: 1px; margin: 0 12px 5px 0; content: ""; background: var(--stage-accent); }
  .device-area { position: relative; width: min(500px, calc((100vh - 32px) * .4771), calc(100vw - 32px)); aspect-ratio: 125 / 262; display: grid; place-items: center; }
  .ambient { position: absolute; border-radius: 50%; filter: blur(80px); opacity: .23; }
  .ambient-one { width: 72%; aspect-ratio: 1; top: 5%; right: -18%; background: #ff6b52; }
  .ambient-two { width: 56%; aspect-ratio: 1; bottom: 2%; left: -15%; background: #6956df; }
  .phone-shell {
    position: relative;
    width: 100%;
    height: 100%;
    padding: 12px;
    border: 1px solid rgba(255,255,255,.23);
    border-radius: clamp(32px, 5vh, 54px);
    background: linear-gradient(145deg, #303138, #111217 62%);
    box-shadow: 0 45px 90px rgba(0,0,0,.55), 0 0 0 5px rgba(255,255,255,.035), inset 0 1px 1px rgba(255,255,255,.26);
  }
  .phone-shell iframe { width: 100%; height: 100%; display: block; border: 0; border-radius: clamp(24px, 3.7vh, 40px); background: #fffaf4; }
  .phone-top { position: absolute; z-index: 5; top: 20px; left: 50%; width: 26%; height: 28px; transform: translateX(-50%); display: flex; align-items: center; justify-content: center; gap: 9px; border-radius: 18px; background: #111217; pointer-events: none; }
  .speaker { width: 46px; height: 4px; border-radius: 4px; background: #2e3037; }
  .camera { width: 7px; height: 7px; border-radius: 50%; background: #293553; box-shadow: inset 0 0 0 2px #141923; }
  .phone-home { position: absolute; z-index: 5; bottom: 19px; left: 50%; width: 25%; height: 4px; transform: translateX(-50%); border-radius: 4px; background: rgba(21,22,24,.7); pointer-events: none; }
  .presenter-signals { position: fixed; width: 1px; height: 1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; }

  .presentation-scene { position: absolute; inset: 0; z-index: 10; overflow: hidden; isolation: isolate; }
  .brand-plate { display: grid; place-items: center; border-radius: 999px; background: #fffaf3; box-shadow: 0 20px 65px rgba(0,0,0,.28); }
  .brand-plate img { width: 100%; height: auto; display: block; }
  .intro-scene {
    padding: clamp(64px, 7vh, 90px) clamp(76px, 7vw, 140px);
    background:
      radial-gradient(circle at 16% 14%, rgba(105,86,223,.23), transparent 29%),
      radial-gradient(circle at 88% 72%, rgba(255,118,87,.22), transparent 28%),
      linear-gradient(118deg, #0a0b10, #13131b 56%, #090a0d);
  }
  .intro-scene::before { position: absolute; inset: 0; z-index: -2; content: ""; opacity: .11; background-image: linear-gradient(rgba(255,255,255,.08) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.08) 1px, transparent 1px); background-size: 88px 88px; mask-image: linear-gradient(90deg, #000, transparent 82%); }
  .presentation-orbit { position: absolute; z-index: -1; right: -12vw; bottom: -32vh; width: min(67vw, 1280px); height: min(78vh, 840px); border: clamp(46px, 6vw, 116px) solid var(--stage-accent); border-radius: 50%; opacity: .86; transform: rotate(-10deg); }
  .brand-plate--intro { position: absolute; top: clamp(58px, 7vh, 82px); right: clamp(76px, 7vw, 140px); width: clamp(270px, 19vw, 365px); padding: 17px 28px; }
  .intro-kicker { margin: 0; color: var(--stage-accent); font-size: clamp(17px, 1.25vw, 24px); font-weight: 820; letter-spacing: .16em; text-transform: uppercase; }
  .intro-thought { position: absolute; top: 50%; left: clamp(76px, 7vw, 140px); width: calc(100% - clamp(152px, 14vw, 280px)); margin: 0; transform: translateY(-48%); color: #fffaf3; font-size: clamp(54px, 4.35vw, 86px); font-weight: 920; line-height: .98; letter-spacing: -.06em; text-wrap: balance; }
  .intro-thought > span { display: block; min-height: 1.04em; white-space: nowrap; }
  .intro-thought > span:last-of-type { color: #ff9b80; font-family: Georgia, "Times New Roman", serif; font-style: italic; font-weight: 650; letter-spacing: -.045em; }
  .hero-fragment { display: inline-block; opacity: 0; transform: translateY(.28em); filter: blur(6px); transition: opacity 320ms cubic-bezier(.22,.8,.22,1), transform 320ms cubic-bezier(.22,.8,.22,1), filter 320ms cubic-bezier(.22,.8,.22,1); }
  .hero-fragment.is-active, .hero-fragment.is-complete { opacity: 1; transform: none; filter: none; }
  .hero-fragment.is-active::after { display: inline-block; width: .085em; height: .76em; margin-left: .11em; vertical-align: -.03em; background: var(--stage-accent); content: ""; animation: cursor-blink .92s step-end infinite; }
  .intro-scene.is-clearing .intro-thought { opacity: 0; transform: translateY(-48%) scale(.985); transition: opacity 650ms ease-in, transform 650ms ease-in; }
  .intro-footnote { position: absolute; left: clamp(76px, 7vw, 140px); bottom: clamp(56px, 6vh, 76px); margin: 0; color: rgba(247,241,232,.58); font: 500 clamp(18px, 1.35vw, 26px)/1.3 Georgia, "Times New Roman", serif; }
  @keyframes cursor-blink { 50% { opacity: 0; } }

  .lecture-scene { background: #0b0c11; }
  .lecture-slide { position: absolute; inset: 0; display: grid; grid-template-columns: minmax(420px, .78fr) minmax(0, 1.22fr); gap: clamp(40px, 4vw, 82px); align-items: stretch; padding: clamp(52px, 6vh, 78px) clamp(64px, 5vw, 104px); background: radial-gradient(circle at 84% 16%, rgba(255,118,87,.17), transparent 26%), linear-gradient(120deg, #12131a, #08090d 64%); overflow: hidden; }
  .lecture-slide::after { position: absolute; z-index: 0; left: -12vw; bottom: -36vh; width: 48vw; height: 54vh; border: clamp(32px,4vw,78px) solid rgba(105,86,223,.42); border-radius: 50%; content: ""; }
  .lecture-copy { position: relative; z-index: 2; min-width: 0; display: flex; flex-direction: column; align-items: flex-start; }
  .brand-plate--lecture { width: clamp(230px, 16vw, 310px); padding: 13px 23px; margin-bottom: auto; }
  .lecture-chapter { margin: 0 0 clamp(22px, 3vh, 38px); color: var(--stage-accent); font-size: clamp(16px, 1.12vw, 22px); font-weight: 840; letter-spacing: .14em; text-transform: uppercase; }
  .lecture-copy h2 { max-width: 11ch; margin: 0; color: #fffaf3; font-size: clamp(48px, 3.55vw, 70px); font-weight: 930; line-height: .92; letter-spacing: -.06em; text-wrap: balance; }
  .lecture-counter { margin: auto 0 0; color: rgba(247,241,232,.44); font: 650 clamp(17px,1.2vw,23px)/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .12em; }
  .lecture-visual { position: relative; z-index: 2; min-width: 0; min-height: 0; margin: 0; display: grid; place-items: center; overflow: hidden; border: 1px solid rgba(255,255,255,.18); border-radius: clamp(24px, 2.4vw, 46px); background: #f7f4ef; box-shadow: 0 38px 80px rgba(0,0,0,.45); }
  .lecture-visual img { width: 100%; height: 100%; display: block; object-fit: contain; }
  .lecture-slide--wide .lecture-visual img { object-fit: cover; }
  .lecture-slide--portrait .lecture-visual { background: radial-gradient(circle at center, #f9f6f0 0 31%, #22232a 75%); }
  .lecture-slide--portrait .lecture-visual img { width: auto; max-width: 74%; object-fit: contain; filter: drop-shadow(0 25px 42px rgba(0,0,0,.32)); }
  .lecture-slide.is-entering .lecture-copy { animation: lecture-copy-enter 720ms cubic-bezier(.42,0,.22,1) both; }
  .lecture-slide.is-entering .lecture-visual { animation: lecture-image-enter 1050ms cubic-bezier(.42,0,.22,1) 80ms both; transform-origin: center; }
  @keyframes lecture-copy-enter { from { opacity: 0; transform: translate3d(-2.2vw,0,0); } to { opacity: 1; transform: none; } }
  @keyframes lecture-image-enter { from { opacity: 0; transform: scale(.9); } to { opacity: 1; transform: scale(1); } }

  .brand-plate--intro, .brand-plate--lecture { border-radius: 0; padding: 0; background: transparent; box-shadow: none; }
  .brand-logo--on-dark img { filter: grayscale(1) brightness(0) invert(1); }
  .brand-logo--on-light img { filter: grayscale(1) brightness(.16); }
  .lecture-slide.theme--light { color: #17121a; background: radial-gradient(circle at 82% 18%, rgba(105,86,223,.14), transparent 30%), #f5efe6; }
  .lecture-slide.theme--light::after { border-color: rgba(255,118,87,.22); }
  .lecture-slide.theme--light .lecture-copy h2 { color: #17121a; }
  .lecture-slide.theme--light .lecture-counter { color: rgba(23,18,26,.45); }
  .lecture-slide.theme--light .lecture-visual { border-color: rgba(23,18,26,.15); box-shadow: 0 35px 70px rgba(67,48,43,.18); }
  .lecture-visual { grid-template-rows: minmax(0,1fr) auto; }
  .lecture-visual figcaption { width: 100%; padding: 16px 22px; color: #332b2a; background: #fffaf3; font: 650 clamp(15px,1vw,20px)/1.3 Georgia,serif; }
  .lecture-layout--horizontal { grid-template-columns: 1fr; grid-template-rows: auto minmax(0,1fr); gap: 26px; }
  .lecture-layout--horizontal .lecture-copy { display: grid; grid-template-columns: auto 1fr auto; align-items: end; gap: 28px; }
  .lecture-layout--horizontal .brand-plate--lecture { width: 250px; margin: 0; grid-row: 1 / span 2; }
  .lecture-layout--horizontal .lecture-chapter { margin: 0 0 8px; }
  .lecture-layout--horizontal .lecture-copy h2 { max-width: none; font-size: clamp(42px,3.2vw,62px); }
  .lecture-layout--horizontal .lecture-counter { margin: 0; align-self: center; }
  .lecture-layout--horizontal .lecture-visual img, .lecture-slide--wide .lecture-visual img { object-fit: contain; }
  .lecture-layout--cinema { grid-template-columns: 1fr; grid-template-rows: minmax(0,1fr) auto; }
  .lecture-layout--cinema .lecture-visual { grid-row: 1; }
  .lecture-layout--cinema .lecture-copy { grid-row: 2; display: grid; grid-template-columns: 230px 1fr auto; align-items: center; gap: 30px; }
  .lecture-layout--cinema .brand-plate--lecture { margin: 0; }
  .lecture-layout--cinema .lecture-chapter { display: none; }
  .lecture-layout--cinema .lecture-copy h2 { max-width: none; font-size: clamp(40px,3vw,58px); }
  .lecture-layout--cinema .lecture-counter { margin: 0; }

  .service-scene { display: flex; flex-direction: column; justify-content: center; padding: clamp(70px,8vh,110px) clamp(82px,8vw,160px); }
  .theme--dark { color: #fffaf3; background: radial-gradient(circle at 86% 16%, rgba(255,118,87,.18), transparent 26%), radial-gradient(circle at 12% 82%, rgba(105,86,223,.18), transparent 28%), #0c0d12; }
  .theme--light { color: #17121a; background: radial-gradient(circle at 88% 18%, rgba(255,118,87,.2), transparent 30%), radial-gradient(circle at 8% 90%, rgba(105,86,223,.11), transparent 34%), #f5efe6; }
  .service-eyebrow { margin: 0 0 clamp(26px,4vh,54px); color: #ff7657; font: 850 clamp(17px,1.2vw,23px)/1 ui-monospace,monospace; letter-spacing: .15em; text-transform: uppercase; }
  .service-scene h2 { max-width: 14ch; margin: 0; font-size: clamp(78px,7.8vw,150px); font-weight: 940; line-height: .84; letter-spacing: -.075em; text-wrap: balance; }
  .service-scene h2 > span { display: block; }
  .service-support { max-width: 36ch; margin: clamp(38px,5vh,66px) 0 0; color: color-mix(in srgb,currentColor 68%,transparent); font: 500 clamp(23px,2vw,38px)/1.25 Georgia,serif; }
  .service-scene.is-entering h2, .service-scene.is-entering .service-support { animation: service-copy-in 700ms cubic-bezier(.22,.8,.22,1) both; }
  @keyframes service-copy-in { from { opacity: 0; transform: translateY(32px); filter: blur(8px); } to { opacity: 1; transform: none; filter: none; } }
  .service-wordmark { align-items: center; text-align: center; }
  .wordmark-motion { position: relative; width: min(1240px,78vw); aspect-ratio: 7819 / 1514; }
  .wordmark-plain, .wordmark-vector, .wordmark-o-layer { position: absolute; inset: 0; width: 100%; height: 100%; }
  .wordmark-plain { display: grid; place-items: center; font: 900 clamp(150px,15vw,292px)/1 Inter,sans-serif; letter-spacing: -.09em; animation: wordmark-plain 3.4s ease-in-out both; }
  .wordmark-vector { object-fit: contain; opacity: 0; animation: wordmark-vector 3.4s ease-in-out both; }
  .wordmark-o-layer { overflow: visible; color: #17121a; }
  .wordmark-o-glyph { transform-box: fill-box; transform-origin: center; animation: wordmark-o-stretch 3.4s cubic-bezier(.65,0,.22,1) both; }
  @keyframes wordmark-plain { 0%,18% { opacity: 1; transform: scale(1); } 26%,100% { opacity: 0; transform: scale(.96); } }
  @keyframes wordmark-vector { 0%,20% { opacity: 0; } 28%,100% { opacity: 1; } }
  @keyframes wordmark-o-stretch {
    0%,20% { opacity: 0; color: #17121a; transform: scaleX(1); }
    28% { opacity: 1; color: #ff7657; transform: scaleX(1); }
    60% { opacity: 1; color: #ff7657; transform: scaleX(3.25); }
    100% { opacity: 1; color: #17121a; transform: scaleX(1); }
  }
  .need-row { display: grid; grid-template-columns: repeat(3,1fr); gap: 28px; margin-top: clamp(50px,7vh,90px); }
  .need-row > div { min-height: 190px; display: grid; grid-template-columns: 72px 1fr; align-items: center; gap: 22px; padding: 30px; border: 1px solid rgba(255,255,255,.18); border-radius: 28px; background: rgba(255,255,255,.06); font-size: clamp(22px,1.6vw,31px); font-weight: 800; }
  .need-row strong { color: #ff7657; font: 900 50px/1 ui-monospace,monospace; }
  .need-row img, .need-row svg { width: 66px; height: 66px; animation: icon-pop 700ms cubic-bezier(.22,.8,.22,1) 450ms both; }
  .need-row img { filter: invert(1); }
  .need-row svg { fill: none; stroke: #ff9b80; stroke-width: 4; stroke-linecap: round; }
  @keyframes icon-pop { from { opacity: 0; transform: scale(.4) rotate(-14deg); } to { opacity: 1; transform: none; } }
  .medallion-demo { display: flex; flex-wrap: wrap; gap: 22px; margin-top: 60px; }
  .medallion-demo span { display: inline-flex; min-height: 84px; align-items: center; padding: 0 30px; border-radius: 999px; color: #fff; background: #8a321e; box-shadow: inset 0 0 0 2px rgba(255,255,255,.2), 0 14px 30px rgba(88,39,26,.18); font-size: clamp(22px,1.6vw,31px); font-weight: 850; }
  .service-joke [data-joke-line="two"] { min-height: 1.25em; margin: 52px 0 0; color: #ff9b80; font: italic 650 clamp(42px,4vw,76px)/1 Georgia,serif; animation: service-copy-in 500ms ease-out both; }
  .search-example { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 34px; margin-top: 72px; padding: 34px 42px; border-radius: 28px; background: rgba(255,255,255,.65); box-shadow: 0 24px 60px rgba(50,32,31,.12); font-size: clamp(22px,1.7vw,34px); }
  .search-example i { color: #ff7657; font-size: 2em; }
  .focus-live-scene { display: grid; grid-template-columns: minmax(460px,.7fr) minmax(0,1.3fr); align-items: center; gap: clamp(45px,5vw,95px); padding: 0 clamp(54px,5vw,96px); }
  .live-meaning { position: relative; z-index: 2; }
  .live-meaning p { margin: 0 0 30px; color: #ff7657; font: 850 20px/1 ui-monospace,monospace; letter-spacing: .14em; text-transform: uppercase; }
  .live-meaning h2 { margin: 0 0 38px; font-size: clamp(70px,6vw,116px); line-height: .84; letter-spacing: -.07em; }
  .live-meaning > span { display: block; max-width: 27ch; color: rgba(255,255,255,.68); font: 500 clamp(22px,1.7vw,32px)/1.3 Georgia,serif; }
  .live-meaning.light > span { color: rgba(23,18,26,.68); }
  .focus-live-scene > iframe { width: 100%; height: 100vh; border: 0; background: #fff; box-shadow: -25px 0 65px rgba(0,0,0,.16); }
  .desktop-medallion-scene { display: block; padding: 0; background: #fff; }
  .desktop-medallion-scene > iframe { position: absolute; inset: 0; width: 100%; height: 100%; box-shadow: none; }
  .live-meaning--cover { position: absolute; inset: 0; z-index: 4; display: flex; flex-direction: column; justify-content: center; padding: 110px 150px; background: radial-gradient(circle at 84% 14%,rgba(255,118,87,.24),transparent 30%),#f5efe6; transition: opacity 650ms ease, visibility 650ms; }
  .desktop-medallion-scene[data-focus-phase="site"] .live-meaning--cover { opacity: 0; visibility: hidden; }
  .focus-phone { justify-self: center; width: min(560px,calc((100vh - 30px)*.4771)); height: calc(100vh - 30px); }
  .invitation-scene { grid-template-columns: .78fr 1.22fr; }
  .celebrity { display: grid; grid-template-columns: 160px 1fr auto; align-items: center; gap: 34px; max-width: 1050px; margin-top: 70px; padding: 28px 34px; border: 1px solid rgba(255,255,255,.16); border-radius: 34px; background: rgba(255,255,255,.07); }
  .celebrity > img { width: 150px; height: 150px; border-radius: 50%; object-fit: cover; }
  .celebrity > div { display: grid; gap: 8px; }
  .celebrity strong { font-size: 38px; }
  .celebrity span { color: rgba(255,255,255,.62); font-size: 23px; }
  .celebrity button { display: flex; align-items: center; gap: 12px; border: 0; color: #fff; background: transparent; font-size: 26px; }
  .celebrity button b { display: grid; width: 78px; height: 78px; place-items: center; border-radius: 50%; color: #ff7657; background: #fff; font-size: 43px; animation: heart-pop 1.2s cubic-bezier(.22,.8,.22,1) 600ms both; }
  @keyframes heart-pop { 0% { opacity: 0; transform: scale(.2); } 65% { transform: scale(1.15); } 100% { opacity: 1; transform: none; } }

  .desktop-scene { position: absolute; inset: 0; z-index: 12; background: #fff; }
  .desktop-scene iframe { width: 100%; height: 100%; display: block; border: 0; background: #fffaf4; }
  .desktop-meaning { position: absolute; inset: 0; z-index: 3; display: flex; flex-direction: column; justify-content: center; padding: 110px 150px; color: #17121a; background: radial-gradient(circle at 84% 14%,rgba(255,118,87,.24),transparent 30%),#f5efe6; transition: opacity 650ms ease, visibility 650ms; }
  .desktop-meaning p { color: #ff7657; font: 850 22px/1 ui-monospace,monospace; letter-spacing: .15em; text-transform: uppercase; }
  .desktop-meaning h2 { margin: 30px 0 50px; font-size: clamp(100px,10vw,190px); line-height: .8; letter-spacing: -.08em; }
  .desktop-meaning span { font: 500 32px/1.3 Georgia,serif; }
  .desktop-scene[data-desktop-phase="site"] .desktop-meaning { opacity: 0; visibility: hidden; }

  .outro-scene { display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: clamp(48px, 7vw, 148px); padding: max(64px, env(safe-area-inset-top)) max(72px, 6vw) max(64px, env(safe-area-inset-bottom)); background: radial-gradient(circle at 84% 49%, rgba(255, 118, 87, .24), transparent 25%), radial-gradient(circle at 12% 8%, rgba(105, 86, 223, .12), transparent 32%), linear-gradient(118deg, #121217 0%, #08090d 58%, #111014 100%); }
  .outro-scene::before { position: absolute; z-index: -1; width: min(38vw, 730px); aspect-ratio: 1; right: max(-16vw, -310px); bottom: max(-27vw, -520px); border: clamp(42px, 5vw, 94px) solid var(--stage-accent); border-radius: 50%; content: ""; opacity: .92; }
  .outro-scene::after { position: absolute; z-index: -1; inset: 0; content: ""; opacity: .12; background-image: linear-gradient(rgba(255,255,255,.07) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.07) 1px, transparent 1px); background-size: clamp(54px,5vw,96px); mask-image: linear-gradient(90deg,#000,transparent 74%); }
  .outro-copy { position: relative; z-index: 1; min-width: 0; padding-bottom: 2vh; }
  .outro-kicker { display: flex; align-items: center; gap: 18px; margin: 0 0 clamp(28px,4vh,56px); color: var(--stage-accent); font-size: clamp(16px,1.25vw,24px); font-weight: 750; line-height: 1; letter-spacing: .16em; text-transform: uppercase; }
  .outro-kicker::before { width: clamp(42px,4.5vw,86px); height: 3px; flex: none; content: ""; background: currentColor; }
  .outro-copy h1 { max-width: 5.2ch; margin: 0; color: #fff9f1; font-size: clamp(112px,14vw,278px); font-weight: 900; line-height: .78; letter-spacing: -.075em; text-wrap: balance; text-shadow: 0 18px 72px rgba(0,0,0,.32); }
  .outro-copy h1::first-letter { color: var(--stage-accent); }
  .outro-support { max-width: 22ch; margin: clamp(38px,6vh,76px) 0 0; color: rgba(247,241,232,.78); font: 400 clamp(24px,2.3vw,44px)/1.2 Georgia, "Times New Roman", serif; letter-spacing: -.015em; text-wrap: balance; }
  .outro-qr-wrap { position: relative; z-index: 1; width: clamp(360px,47vmin,560px); aspect-ratio: 1; filter: drop-shadow(0 36px 68px rgba(0,0,0,.48)); }
  .outro-qr { width: 100%; height: 100%; display: block; object-fit: contain; }
  .outro-scene.is-entering .outro-copy { animation: outro-copy-enter 760ms cubic-bezier(.42,0,.22,1) both; }
  .outro-scene.is-entering .outro-qr-wrap { animation: outro-qr-enter 940ms cubic-bezier(.42,0,.24,1) 90ms both; transform-origin: 50% 50%; }
  @keyframes outro-copy-enter { from { opacity: 0; transform: translate3d(-2.5vw,0,0); } to { opacity: 1; transform: none; } }
  @keyframes outro-qr-enter { from { opacity: 0; transform: translate3d(0,2.5vh,0) scale(.82); } to { opacity: 1; transform: none; } }

  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { animation-duration: .01ms !important; animation-delay: 0ms !important; animation-iteration-count: 1 !important; scroll-behavior: auto !important; }
    .hero-fragment.is-active::after { display: none; }
  }
  @media (max-width: 900px) {
    .intro-thought > span { white-space: normal; }
    .lecture-slide { grid-template-columns: 1fr; grid-template-rows: auto minmax(0,1fr); gap: 26px; padding: 34px; }
    .brand-plate--lecture { width: 190px; margin-bottom: 24px; }
    .lecture-copy h2 { max-width: 16ch; font-size: clamp(40px,8vw,68px); }
    .lecture-counter { position: absolute; top: 0; right: 0; }
    .outro-scene { grid-template-columns: 1fr; justify-items: center; gap: 34px; padding: 42px 28px; text-align: center; }
    .outro-kicker { justify-content: center; margin-bottom: 24px; }
    .outro-copy h1 { max-width: none; font-size: clamp(72px,22vw,136px); }
    .outro-support { margin: 24px auto 0; font-size: clamp(20px,5vw,30px); }
    .outro-qr-wrap { width: min(52vh,76vw); }
  }
</style>
