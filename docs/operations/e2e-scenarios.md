# E2E Scenario Index

Канонический реестр E2E/BDD сценариев проекта.

Focus email has two distinct gates. The fixed
`focus-agent-e2e@kenigevents.ru` + fresh admin-issued OTP runner is an internal
deployed Auth/membership integration check only. The external delivery E2E must
run manually in a protected GitHub Environment, receive a random OTP through
the dedicated no-persistence Yandex Mail Trigger → API Gateway WebSocket (or
the optional controlled read-only IMAPS adapter), and must not use a service
key, admin link or fixed OTP. An ambiguous selected-once issuance keeps code
entry open while resend is cooling down.

The implemented external gate is `.github/workflows/external-focus-email-otp.yml`
and its operating contract is [`docs/testing/external-focus-email-otp.md`](../testing/external-focus-email-otp.md).
Routine runs reuse one fixed mailbox and therefore do not create a new Auth user;
fresh-user aliases are explicit, non-routine coverage.

`focus.otp.ios_keyboard_preflight` is the side-effect-free iOS control scenario: it stabilizes only the exact allowlisted Safari first-run dialog, proves empty control email/numeric keyboards, then the empty product email keyboard, and requires issue/verify/registration `0/0/0`. The first valid native-first receipt is [run 30767191144, attempt 2](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30767191144): exact dialog observed/dismissed once, stable absence and all three keyboard checks PASS, `0/0/0`. The proven control path is native `com.apple.mobilesafari` + `respectSystemAlerts=true`, exact current-alert action, then WebKit attachment; WebKit-first and fuzzy/global-button handlers are forbidden. Its capability/startup implementation is now the neutral `site/e2e/mobile-web/` transport consumed by OTP and Search; new feature adapters must not copy it. The full iOS journey embeds the same control preflight in its session; terminal [run 30772233868](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30772233868) therefore supersedes repeated preflights for this harness SHA. Repeat the standalone preflight after a direct iOS startup/keyboard harness change, not as a ritual before every OTP send.

`focus.otp.browser_tab` uses one semantic journey across protected Chromium,
Android Chrome/UiAutomator2 and iOS Safari/XCUITest jobs. Terminal receipts and
the per-platform implementation boundary are recorded in the external runbook
and scenario registry; a workflow skeleton or non-terminal mobile run is not
acceptance. Immutable SHA `4a19fbe0b243d8a9a4652ff0c1e4fee9e895cf9c`
has terminal PASS receipts for Chromium
[30745526613](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30745526613)
and Android
[30747598046](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30747598046).
iOS terminal run
[30754894934](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30754894934)
is retained as corrected `BLOCKED_SAFARI_FIRST_RUN_UI` with zero mail/Auth/membership side effects. The visible first-run search-engine dialog invalidates every keyboard conclusion from that run; it is not acceptance.

Fault preview SHA `3e892bd510818c07b2e14b708db7d5f39e2ae845` has terminal
`client_supabase_direct_unreachable` PASS receipts for Android
[30772062840](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30772062840)
and iOS
[30772233868](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30772233868).
Each artifact proves one matching mail, exact `issue=1`, `verify=1`,
`registration=1` with registration `200`, relay as the final route for all
three mandatory operations, an active direct-host fault, confirmed membership,
returning state and redaction PASS. The iOS receipt additionally proves the
search-choice dialog was observed and dismissed exactly once and all control/product
email/numeric keyboards passed.

The reciprocal fault preview SHA
`592ffb2d5a68615b65481ee1acea65b728af8d8d` has terminal
`client_yandex_relay_unreachable` PASS receipts for Android
[30772957771](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30772957771)
and iOS
[30773125445](https://github.com/onedayonemasterpiece/events-bot-new/actions/runs/30773125445).
Both artifacts again prove one mail and exact `1/1/1`, registration `200`,
membership + returning state and redaction PASS, while issue, verify and
registration each have one `direct` final outcome and no relay outcome. Thus
both one-client-route-down directions are closed on both mobile platforms;
the both-routes-down/no-mail degraded cell remains separate.

The same scenario now has an explicit transport contract and immutable
build-time fault profiles. The first faulted mobile release cell is
`client_supabase_direct_unreachable`: direct Supabase probes are rejected
before selected-once dispatch, while issue/verify/registration must each use
the Yandex relay exactly once. Canonical evidence adds sanitized fault and
transport event streams; workflow success without an activated matching fault
receipt is `FAIL_FAULT_NOT_ACTIVE`, not PASS.

Правило обновления:
- При добавлении нового `.feature` файла или существенном изменении набора сценариев сначала обновляй эту страницу.
- Для сценариев с «живыми» внешними ссылками отмечай, насколько быстро они устаревают.

## Сценарии

| Feature file | Назначение | Тип ссылок/данных | Примечание по актуальности |
|---|---|---|---|
| [`scripts/smoke_private_events_mcp.py`](../../scripts/smoke_private_events_mcp.py), [`tests/test_private_events_mcp_*.py`](../../tests/) | Private Events MCP: раздельные OAuth/PKCE journeys ChatGPT и Codex, семь read-only evidence tools для Codex, а также явно разрешённые generic Telegram/VK text read/prepare/publish tools только для ChatGPT | Synthetic provider fakes in CI; live production event/incident reads and explicitly allowlisted Telegram/VK aliases after exact-main deploy | Production acceptance requires exact resource/client isolation, DB unchanged, `provider_calls=0` for evidence queries, strict target aliases, redacted untrusted provider text, prepare/commit binding, and no live publication unless the operator supplied the exact test text and target. Provider timeout is outcome-unknown and must not be retried with a new idempotency key. MAX/media/edit/delete remain out of scope. |
| [`site/e2e/search/run.mjs`](../../site/e2e/search/run.mjs), [`.github/workflows/static-site-search-canary.yml`](../../.github/workflows/static-site-search-canary.yml) | Authenticated semantic Search: three incident queries, one POST, terminal cards/IDs, real scroll, pagination/dedup, cache repeat and validation zero-POST | Latest durable immutable secret candidate + real Supabase Auth/RLS/Edge/pgvector; no mailbox | Exact target SHA and catalog/corpus revisions are mandatory. Browser uses terminal `session_fixture`; its scroll receipt is accepted only after real wheel input from the document top brings the final rendered card into the viewport (not after a fixed gesture count or DOM scrolling). Android/iOS get a fresh callback/session per device and native keyboard/touch. They reuse `site/e2e/mobile-web/`: iOS input focus is an exact native accessibility tap plus native keyboard observation; shared `performNativeDocumentSwipe` resolves native device dimensions, routes Android Chrome to absolute-coordinate W3C touch and iOS Safari to XCUITest application-level `mobile: swipe`, then restores the same WebView before DOM assertions. WebView/CSS geometry and native-control shortcuts `mobile: scrollGesture`/`mobile: scroll` are forbidden; Safari W3C pointer actions are also not accepted because live run `31277971410` proved they can acknowledge 24 times without moving WebKit DOM. Android terminal run `31274932745` proves all three queries, cache/pagination and real native scroll on the accepted target. iOS may restart Appium once only when the closed startup receipt proves the first WebDriver session failed before callback and Search traffic; the still-unconsumed device callback is reused and raw Appium logs are deleted. XCUITest Safari's exact unsupported hide-keyboard response is best-effort only on iOS and can pass solely if the following native scroll still produces a positive DOM delta and reveals the final card. Manual `live_consistent + cached_vector` may bootstrap one invalidated-cache miss but must then prove an immediate zero-provider hit; scheduled/post-deploy `release_exact` still requires the first request to hit. Cached 30-minute, cold 3-hour, bounded LLM four/day, mobile nightly and immediate post-deploy schedules are separate from Focus real-mail OTP. Artifacts are accepted only with `.redaction-ok`; bearer target, query text, email, OTP, session, HAR/video/source are forbidden. |
| [`.github/workflows/static-collections-quality-e2e.yml`](../../.github/workflows/static-collections-quality-e2e.yml), job `facts-v3 offline contract` | Offline regression gate для единственного Smart Update facts-v3 call, merge/client/backfill и hash-bound Gate-B evaluator | Synthetic SQLite/source-bound fixtures; без provider/LLM и без production writes | При изменениях `smart_event_update.py`, `google_ai/client.py`, facts backfill/evaluator/schema или любого из пяти точных test-файлов запускает один фиксированный pytest suite. Это contract gate, не real EventSource replay и не разрешение semantic publication. |
| `npm --prefix site run test:focus-group-product` + live invite/email/Yandex checklist in [`static-site-focus-group`](../features/static-site-focus-group/README.md) | Focus invitation QR/link, 30-day participation, independent account logout, resilient Auth transport and identity choices | Deterministic local contracts + real Supabase Auth, Postbox/automation inbox, Yandex OAuth and phone connectivity routes for live closure | Local contracts are necessary but not sufficient. Live closure must decode the published QR, join through the exact fragment URL, receive and consume a one-time email link in a dedicated automation inbox, verify a separate six-digit-code issuance, link/enter through a real Yandex consent round-trip, verify one account/membership, logout, and repeat enrollment. The clean-retest URL must remove the actual project Auth key, chunks, verifier and intent before redirect and the next screen must show no former identity. The generated candidate must expose both direct and relay URLs; absence of relay is a release failure. Before upload, direct and relay Data/Function nonce probes must each return HTTP 200, the exact nonce and `schema=1`. `/fokus-gruppa/diagnostika-ustoychivost/` must compare direct reads with framework reads on a reproduced failing phone route. OTP issue/verify/refresh uses one preselected route and is never blindly repeated through the alternate route after an ambiguous result; verified participant registration is idempotent and may recover once through the alternate route. The email request must resolve the clean fragment-free path/query against `window.location.origin` before passing the absolute same-origin `emailRedirectTo` to Supabase. External code agents use a fresh operator-issued OTP/one-time link for the dedicated test identity; a static fixed bypass is forbidden. Never substitute a mocked callback or Supabase default SMTP for the human-delivery E2E. |
| [`tests/test_unusual_events_golden_contract.py`](../../tests/test_unusual_events_golden_contract.py) + [`site/tests/unusual-events-source-contract.test.mjs`](../../site/tests/unusual-events-source-contract.test.mjs) | R01–R10 frozen unusual taxonomy/evaluation and cross-source contracts | Checked 2026-07-27 production snapshot facts + repository source | The JSON fixture covers all 15 families, at least 20 hard negatives, explicit non-events and repeated concept/series groups. The final integrated focused semantic/builder suite must additionally prove explicit canonical eligibility, ordinary-corpus receipt/hash binding, durable notification state and `provider_calls=0`. These fixture/source tests are not Kaggle canary evidence. |
| [`site/tests/unusual-events.playwright.mjs`](../../site/tests/unusual-events.playwright.mjs), `UNUSUAL_EVENTS_PLAYWRIGHT_MODE=product` | Packaged mobile R15 product journey: home ≤30, Collections, Free sticky identity, horizontal 5:4 crop, far calendar, noindex Favorites and concept-deduplicated `/neobychnoe/` | Immutable noindex generated candidate | Requires only `UNUSUAL_EVENTS_BASE_URL=<immutable-prefix>` (plus `UNUSUAL_EXPECT_APPROVED=1` after a real approved semantic build). Product mode never opens lab routes; the candidate must return `404` for `/lab/unusual-unread/new-core/`. It rejects ordinary-card substitution and page-view provider calls. The `11d8c984` run is superseded. Current immutable acceptance candidate: repo SHA `17d3d03db763c3a537f653dce6cb806b7bf05d4a`, run `static-site:production-secret-20260728T220313-83376181:a48b2eb4cd41`, prefix `https://kenigevents.ru/_review/nf9iGJEWKfDn27uUFeAX_j9g-xGi8OHYxcYXzFEcEQs/`. This candidate is noindex and does not imply production-root promotion. |
| [`site/tests/unusual-events.playwright.mjs`](../../site/tests/unusual-events.playwright.mjs), `UNUSUAL_EVENTS_PLAYWRIGHT_MODE=lab` | Ten-state red-dot runtime matrix: baseline/new-core/adjacent/view/reload/same-series/migration/failure/exact aria-label/mobile drawer | Separately served local noindex Astro lab | Requires only `UNUSUAL_EVENTS_LAB_BASE_URL=<local-root>`. It is a deterministic local runtime contract, never candidate/public acceptance. `all` mode may run both suites only when both independent base URLs are supplied. |
| [`site/scripts/check-listing-desktop-geometry-playwright.sh`](../../site/scripts/check-listing-desktop-geometry-playwright.sh) | Shared CSS and sticky-stack contract for desktop Today, Tomorrow, Weekend and Popular | Immutable preview/candidate | Executable Playwright gate at 1366×768, 1536×864 and 1920×1080; rejects a missing listing bundle, a non-sticky/covered 57px global header, rail overlap, unbounded media and horizontal overflow. Requires `STATIC_SITE_REVIEW_BASE_URL` and `playwright-cli`. |
| [`site/tests/popular-desktop-listing.test.mjs`](../../site/tests/popular-desktop-listing.test.mjs) + `site/scripts/check-preview.mjs` | Desktop Popular V28 availability, family dedup, temporal context and warm-only 4+1 continuation | Static preview fixture + localStorage profile fixtures | Static gate verifies five ordered evidence shelves, 3–5 truthful cards per rendered shelf, no global/personal family overlap, repeat folding, elapsed-event exclusion, cold-start absence and unchanged large/adaptive mobile order; unit test covers no-consent, two-signal, 4+1 and fail-closed underfill states. |
| [`site/scripts/check-desktop-cta-geometry-playwright.sh`](../../site/scripts/check-desktop-cta-geometry-playwright.sh) | Browser geometry contract for desktop event CTA families | Immutable preview/candidate; four frozen/real event specimens | Executable Playwright gate at `1536×864` (FHD at 125%): two Split portrait/OCR specimens must keep one measured compact row; two Editorial wide-photo specimens must keep the tall three-row card with the calendar/share/like row at the bottom. Requires `STATIC_SITE_REVIEW_BASE_URL` and `playwright-cli`; rejects wrong family routing, wrapping, overlap, overflow and missing controls. |
| `npm --prefix site run check:browser-release -- --browser chromium --root <generated-root> --report <report.json> --artifact-dir <evidence-dir>` | Blocking generated-candidate visual and mixed-input release journeys | Every checked production-root proof and immutable noindex secret candidate | Serves the real generated tree at `1536×864` without navigation interception. It waits for lazy-image `decode()`, requires every `visual_only` hero/gallery slide and every compact recommendation image to use `cover`, rejects any visible fallback/band, independently limits very-tall OCR/document crop to `20%`, and measures equal media plus total-card heights per optimizer row. It retains settled hero/section/viewport screenshots, checks the canonical component in `Смотрите дальше`/`Ещё события`, covers cold/reload BODY hero arrows, real inert mouse click + physical Russian-layout `L/K/S/Enter`, header/editor and single-image negatives, real gallery cross-document Enter, and footer `P`/`S` after pointer scroll. The festival page must expose exactly the release manifest's positive `sqlite-festival-calendar-v1` rendered count at desktop and mobile, with no more cards than the source ledger and one loaded image per rendered card; ended editions may reduce the rendered subset without weakening those invariants. Repeat with `--browser firefox` / `--browser webkit` on a host with Playwright Linux dependencies; WebKit does not replace native Safari acceptance. A successful Chromium receipt is required before archive trust/publication. |
| [tests/e2e/features/static_site_personalization.feature](../../tests/e2e/features/static_site_personalization.feature) | Static-site anonymous personalization contract | Fixture/demo + secret candidate static site | **Draft, не runnable Behave E2E:** feature помечен `@draft`, step definitions отсутствуют. Release-critical runnable contracts находятся в `tests/test_static_site_release.py`, `tests/test_static_site_build_handoff.py`, `tests/test_static_site_public_gate.py`, `site/tests/event-detail-runtime-regressions.test.mjs`, `site/tests/personal-feed-surface.test.mjs` и `site/scripts/static-release.behavior.test.mjs`. Public candidate gate дополнительно проверяет единый service footer, ровно шесть desktop continuation cards без load-more, mobile fallback absence, стабильную related-card skeleton geometry и CTA-family geometry; public secret-candidate acceptance и production-root cutover остаются разными gates. Полный inventory: `docs/features/static-site-pages/test-scenarios.md`. |
| [tests/e2e/features/telegram_monitoring.feature](../../tests/e2e/features/telegram_monitoring.feature) | Telegram monitoring + Smart Update (multi-source, facts/log/Telegraph) | Живые TG-посты | Требует периодического обновления контрольных ссылок и очистки данных перед прогоном |
| [tests/e2e/features/telegram_sources_seed.feature](../../tests/e2e/features/telegram_sources_seed.feature) | Проверка канонических Telegram-источников (seed + список + пагинация) | Локальная БД + UI | Стабильный, но зависит от UI `/tg` и `sources.yml` |
| [tests/e2e/features/smart_event_update.feature](../../tests/e2e/features/smart_event_update.feature) | Проверка merge/dedup/conflict логики Smart Update | Преимущественно локальные фикстуры + точечные живые проверки | Стабильный, но отдельные live-кейсы нужно периодически ревизовать |
| `scripts/e2e/run_vk_auto_import_live.py` | Bounded production VK auto-import через реальный Telegram UI и Smart Update | Живой VK inbox + production bot | Запускать 1–3 раза с `--limit 1`, только через `TELEGRAM_AUTH_BUNDLE_E2E`; сохраняет ответы/редактирования в ignored artifact, немедленно падает на `Not authorized` / operator error и по умолчанию не принимает `queue processed: 0/0` за Smart Update E2E (`--allow-empty` допустим только для отдельно маркированного auth smoke). Ранее перечисленные Behave-файлы `vk_auto_queue.feature` и `multi_source_vk_tg.feature` отсутствуют и остаются явным техдолгом. |
| [tests/e2e/features/dom_iskusstv.feature](../../tests/e2e/features/dom_iskusstv.feature) | Поток /parse для Дом искусств | Живой источник + локальная БД | Требует проверки доступности источника и токенов |
| [tests/e2e/features/festival_parser.feature](../../tests/e2e/features/festival_parser.feature) | Фестивальный парсер и публикация | Живые сайты/данные | Периодически ломается от изменений внешних сайтов |
| [tests/e2e/features/festival_queue.feature](../../tests/e2e/features/festival_queue.feature) | Очередь фестивалей (handoff из VK auto import и Telegram Monitoring → Festival Queue → Festival Parser, индексы и иллюстрации) | Живые VK/TG/сайты | Требует актуальных ссылок на посты (VK: wall-132625599_16376, TG: @world_ocean_museum/11572) и проверку страницы «Фестивали» |
| [tests/e2e/features/festival_queue_prefilled.feature](../../tests/e2e/features/festival_queue_prefilled.feature) | Очередь фестивалей на уже заполненной `festival_queue` (накопленные данные из массовых прогонов, snapshot 20–21.02.2026) | Snapshot БД + live Telegraph | Стабильнее, чем live-ссылки: сценарий явно выставляет статус `pending` для контрольных URL; TG-прогон ограничен `--limit=2` (каноничные посты `kaliningradartmuseum/7589` и `klassster/17700`) чтобы не зависеть от нового хвоста очереди |
| [tests/e2e/features/channel_nav.feature](../../tests/e2e/features/channel_nav.feature) | Навигация и UI-кнопки бота | Локальный бот | Стабильный, низкая зависимость от внешних данных |
| [tests/e2e/features/admin_action_assistant.feature](../../tests/e2e/features/admin_action_assistant.feature) | Admin Action Assistant: RU‑запрос → команда → подтверждение (/assist,/a, кнопка меню) | Live UI + LLM (Gemma) | Стабильный smoke: использует только безопасные команды; зависит от доступности Google AI и allowlist ассистента |
| [tests/e2e/features/guide_excursions.feature](../../tests/e2e/features/guide_excursions.feature) | Guide excursions monitoring: `/guide_excursions` → Kaggle-first full scan → `/guide_report`/`/guide_runs` с `llm_ok/deferred` → preview → `/guide_events`/`/guide_recent_changes`/`/guide_template` → `/guide_facts`/`/guide_log` по нескольким control occurrences → publish digest во все каналы из `GUIDE_DIGEST_TARGET_CHATS` | Live TG + Kaggle + target channel(s) | Live manual scenario: требует Telethon auth, `GOOGLE_API_KEY2`, Kaggle creds и Bot API publish access во всех каналах из `GUIDE_DIGEST_TARGET_CHATS` (текущие runtime targets: `@wheretogo39`, `@youwillsee39`; для изолированного E2E лучше временно переопределить safe test channel или список каналов). В UI нужно видеть `transport=kaggle`, финальный `ops_run_id`/`/guide_report`, completion/report с LLM counters, в preview — подсказки `facts=/guide_facts <id>` и `log=/guide_log <id>`, а перед publish обязательно проверяются несколько control excursions через admin surfaces и хотя бы один `GuideTemplate` через `/guide_template <id>`. Сценарий считается успешным только на success-marker `✅ Мониторинг экскурсий завершён` и `/guide_report ... status=success`; `... завершён с ошибками`, local fallback note, отсутствие Kaggle notebook logs или publish без materialized media считаются инцидентом. Финальный publish-check допускает оба канонических path: либо `album -> text post(s) -> linked caption`, либо compact single-caption album, если весь digest поместился в caption. Если в scheduled path после успешного full scan новых digest-ready карточек нет, это тоже проверяемый канонический outcome: оператор должен получить bot-only ack про пустой digest, а target channels должны остаться без служебного `новых находок нет` поста. Если тестируется backfill нового target channel, альбом должен сохранять album grouping и caption уже должен ссылаться на текстовые посты в этом же целевом канале либо оставаться single-caption album без лишнего follow-up post. Для repeatable E2E horizon по умолчанию сужается до `GUIDE_DAYS_BACK_FULL=5`, `GUIDE_DAYS_BACK_LIGHT=3` и `GUIDE_SCAN_LIMIT_FULL=25`, а `GUIDE_EXCURSIONS_LOCAL_FALLBACK_ENABLED=0`, чтобы ручной прогон проверял именно Kaggle/Gemma flow, а не глубокий исторический хвост или скрытый local fallback. Канонический fresh mass rerun 16.03.2026 подтвердил этот path на `10` источниках (`sources=10`, `posts=36`, `llm_ok=21`, `created=10`) с публикацией `7` карточек после runtime dedup |
| [tests/e2e/features/bot_scenarios.feature](../../tests/e2e/features/bot_scenarios.feature) | Базовые smoke-флоу бота | Локальный бот | Стабильный базовый smoke-набор |
| [tests/e2e/features/release_smoke_smart_update.feature](../../tests/e2e/features/release_smoke_smart_update.feature) | Smoke релиза: /tg (trust+festival), VK auto import (limit=10), псевдо‑фестиваль «Масленица» из holidays, Telegram Monitoring (>=10), Telegraph‑картинки (Supabase WebP / Catbox fallback), ticket-sites queue (Pyramida), /start→«Добавить событие» (Smart Update source-log), добавление фестиваля | Live UI (VK/TG/Kaggle/Telegraph) | Перед запуском обновить ENV/доступы и убедиться, что есть реальные события/посты в окне сканирования; сценарий по «Масленице» использует VK посты: wall-53460968_11095, _11053, _11099, wall-41284227_7045; часть сценариев помечена `@manual` (даты /parse, ручные флоу) |
| [tests/e2e/features/release_multisource_control.feature](../../tests/e2e/features/release_multisource_control.feature) | Контроль multi-source (VK+TG+/parse) под дату релиза | Live + DB‑проверки | Шаблон/ручной сценарий для финальной валидации “один event_id из трёх источников”; требует актуальных данных в БД |

## Live-only чеклист

- Перед прогоном сценариев на реальных постах очищать следы прошлых запусков (события/сканы/очереди).
- Для VK auto queue использовать свежий snapshot prod БД (не старше 6 часов).
- Проверять, что из отчёта доступны:
  - ссылка на Telegraph,
  - ссылка на `/log <event_id>`,
  - видимость события в нужном списке (`/events`, `/exhibitions` и т.д.).
- Для постов об отмене/переносе проверять, что событие помечается неактивным (`event.lifecycle_status=cancelled|postponed`) и исчезает из month/weekend страниц после rebuild.
