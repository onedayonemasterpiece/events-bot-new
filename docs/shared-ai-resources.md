# Shared AI resources

Canonical feature documentation: [LLM gateway Live resources](features/llm-gateway/live-resources.md).

This checkpoint adds a tested backend resource boundary, not an enabled browser Live search. Existing runtime and ordinary quota accounting are unchanged. Activation depends on the compatible shared transport, verified Live limits and authorized private-package installation.
