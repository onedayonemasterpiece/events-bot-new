Нужна короткая, но обязательная корректировка твоего ответа по точным DOM-фактам и требованиям. Не меняй FAIL/PASS только ради согласия; исправь противоречия.

1. В реальном DOM `24 идеи` УЖЕ является anchor с `href=/vyhodnye/`; это подтверждено capture-results.json. Поэтому фраза «Если H1 не кликабелен» неприменима. Оцени именно существующий linked fragment: оставить underline, заменить его более editorial affordance или расширить hit target? Ответь одним решением.
2. `frequently_forwarded` намеренно говорит об одном конкретном событии. Не переписывай его во множественное число ради общего URL: правильный продуктовый вопрос — должен ли linked fragment/CTA вести прямо на `{event_url}`. Дай конкретное решение.
3. Требование festival — назвать конкретный фестиваль. Твоя версия «главный фестиваль выходных» всё ещё абстрактна и нарушает требование. Дай шаблон с `{festival_name}` и `{festival_url}`, укажи, какая часть H1 кликабельна.
4. Погодная storm-концепция — именно последовательная цепочка в 2–3 захода, а не один экран с двумя кнопками. Разложи на 3 последовательных экрана: weather premise -> `{lecture_1}` -> `{lecture_2}`, по одному primary CTA/linked object на экран и public Next как secondary.
5. Уточни CTA recommendation: действительно ли semantic CTA должен быть solid-button в этой типографической editorial-системе, или достаточно явно более сильной editorial link, пока `Показать следующее` остаётся muted ghost? Выбери одно и объясни одним предложением. Не противоречь требованию «один primary CTA».
6. Уточни vertical rule численно без внутреннего противоречия: stage высота при 1366×768 и 1440×900, расстояние actions->stage bottom и ожидаемые y-range H1/actions/categories. Всё должно оставаться <=50vh и категории видны.

Ответь только разделами 1–6, по-русски. Это corrective continuation того же visual gate, не новый независимый review.
