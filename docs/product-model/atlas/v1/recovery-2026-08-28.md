# Product Atlas recovery after UI SoT advance — 2026-08-28

- Recovery ID: `product-atlas-recovery-2026-08-28`
- Status: `GIT_RECOVERED_PENPOT_TARGET_BINDING_PENDING`
- Product authority: `onedayonemasterpiece/events-bot-new`
- Penpot reads/writes in this recovery: `0 / 0`

## Product result

Restore the Product Atlas input after the UI source of truth advanced, so product work can proceed in parallel with unfinished design-system normalization.

The product projection does **not** wait for every component family or page archetype to become promoted. It uses stable Git identities and presents incompleteness explicitly:

```text
accepted product meaning
+ source-proven AS-IS UI baseline
+ active owner-review delta
+ active Astro/UI implementation candidates
+ product hypotheses
+ unresolved evidence and UI gaps
```

A separate Penpot account and a separate Product Atlas file will be used later. The existing design-system Penpot file and its IDs are evidence only; they are not target bindings for the new account.

## Effective authority layers

### 1. Product truth

Current product repository source cut:

`events-bot-new@8710e56fa3685f6c30a90cd062d532dce0348cce`

The existing v1 registries remain the semantic base. This recovery adds a current source lock, a delta layer and a visualization handoff instead of rewriting stable IDs.

### 2. Corrected semantic base

`lovekgd-design-system#50@9b8043f3bdb86fab4eee00bf94b0f10d4f029c50`

This remains the corrected semantic origin for the 17 archetypes. It is not treated as the latest complete UI state by itself.

### 3. Closed AS-IS round-trip baseline

`lovekgd-design-system#52@b86bab3e91511b3d4bd7d953b22bceb847f02a51`

Its deterministic Product Atlas handoff proves:

- 17 archetypes;
- 34 desktop/mobile review cases;
- 97 semantic regions;
- 97 visual patterns;
- 75 component identities;
- 180 screen states;
- zero orphan design IDs.

This is the stable site-as-is visualization substrate. It does not prove owner acceptance, promotion or user outcomes.

### 4. Active owner-review delta

`lovekgd-design-system#53@47d0fef53c33200492d92f6a086d9b8813fe187e`

The review ledger remains `IN_PROGRESS`. It contains source-proven corrections and unresolved owner-review work. Relevant current examples include:

- `ListingDiscoveryRail@6` / Weekend Floating Island — `READY_FOR_OWNER_REREVIEW`, `processed: NO`;
- exact seven-artifact Collection 1 — `READY_FOR_OWNER_REREVIEW`, `processed: NO`;
- Home HeroTalk Photo Mosaic mechanism — `READY_FOR_OWNER_REREVIEW`, `processed: NO`;
- mobile rail media parity — `READY_FOR_OWNER_REREVIEW`, `processed: NO`;
- global component-lineage closure remains an evidence gate.

These deltas are visualized as an active review layer, not silently promoted into the accepted baseline.

### 5. Active Astro/UI candidate

`events-bot-new#596@49c351873d40a2ea55f0a32837c7376e344d9c17`

This candidate contains source-proven owner-audit corrections, including:

- `ListingDiscoveryRail@6` with `plane | floating-island`;
- migrated Date, Popular and Weekend consumers;
- Weekend as the first content-sized Floating Island consumer;
- exact seven-artifact Collection 1 candidate;
- corrected Home HeroTalk and mobile rail media evidence.

It remains a Draft candidate until merge/release/owner acceptance evidence exists.

### 6. Product hypotheses

`events-bot-new#587@f78e7c5974b4192bddf9eea901ee6d8b57f51560`

Canonical location data and agent-assisted event discovery are retained as product hypotheses/research candidates. They are not rendered as accepted capabilities or current production behavior.

## Restored Git artifacts

This recovery adds:

- `current-source-lock.2026-08-28.v2.json` — effective source layers and supersession;
- `product-delta.2026-08-28.v2.json` — entity updates and new source-grounded findings/decisions/gaps;
- `visualization-handoff.2026-08-28.v1.json` — deterministic input for later Product Atlas materialization;
- `scripts/validate_product_atlas_recovery_20260828.py` and its test;
- cross-repository CI readback of the exact baseline, review and Astro candidate refs.

The original v1 files are preserved as the historical semantic base. The recovery layer is the current authority for changed facts.

## Later Penpot entry gate

When the separate Penpot account is connected:

1. fresh-read the exact Git heads again;
2. create or select a separate Product Atlas file in that account;
3. do not import or mutate the design-system Penpot file;
4. materialize only the views declared in the visualization handoff;
5. keep baseline, active-review, hypothesis and unresolved layers visually distinct;
6. read back real file/page/board/object IDs;
7. commit a versioned MCP receipt to Git;
8. only then replace `binding_pending` placeholders.

No existing Penpot UUID is reusable as a target ID merely because it appears in design-system evidence.

## Completion boundary

This Git recovery is complete when its targeted validators are green. It does not claim:

- that design-system normalization is finished;
- that all owner comments are processed;
- that candidate Astro changes are merged or deployed;
- that user or owner outcomes are proven;
- that Product Atlas has already been materialized in Penpot.
