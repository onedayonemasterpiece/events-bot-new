interface:
  display_name: "Static Event Medallions"
  short_description: "Research and build KenigEvents medallions"
  default_prompt: "Use $static-event-medallions to research, source, render, and verify a KenigEvents static event medallion."
policy:
  allow_implicit_invocation: true
